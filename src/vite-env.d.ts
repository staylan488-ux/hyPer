/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Injected at build time — shown in Settings so deployed version is verifiable. */
declare const __BUILD_ID__: string;
