# Handoff

Updated: 2026-07-20 (rev 70: Phase 1 done, first Xcode build green, Phase 2 features landed)

## Rev 70 — Phase 1 executed: recovery push, reconciliation, first Apple build, Phase 2 features

### Session results after the merge (same day)

- `762b1a84` — fixed the first REAL Xcode compile errors (syntax parsing had missed all three): availability-gated stored property in HyperRunPlugin replaced with type-erased storage + iOS 17 computed accessor; `requestPermissions` overrides in HyperRunPlugin/HyperTimerPlugin; VisionKit DataScanner calls hopped to MainActor in HyperBarcodePlugin.
- **First successful Apple-framework build**: `xcodebuild -scheme App -destination generic/platform=iOS Debug CODE_SIGNING_ALLOWED=NO` → BUILD SUCCEEDED (Xcode 26.6, iOS 26.5 SDK). The iOS 26.5 platform component was downloaded/installed via `xcodebuild -downloadPlatform iOS` (8.5 GB). Xcode license already accepted. Signing/physical device still require the Account Holder's team.
- `5e172d8e` — saved-barcode-first pipeline: scans resolve owner catalog → USDA → Open Food Facts; total miss offers "Create a saved product for this barcode" (manual/label entry bound via foods.external_source='barcode', external_id=code, so the next scan resolves locally). New `src/lib/savedBarcodeCore.ts` + `savedBarcodeFoods.ts`, provenance labels, `barcode_saved` log source. No migration needed (foods.external_source/external_id already exist). Provider priority deliberately NOT reordered pending the 50-100 product benchmark.
- `6069d2b2` — WHOOP durable deletion: History delete now tombstones (dismissed_at) any session WHOOP knows about — whoop-source sessions and GPS/manual sessions with linked whoop segments (hard delete orphaned segments via ON DELETE SET NULL and re-sync resurrected the workout). Fail-safe dismissal on check error. Absorb path keeps hard delete intentionally. Regression test added (dismissed-GPS-host).
- `708ea277` — Body weight surface in You/Settings: latest-weight hero with persisted lb/kg toggle, measured time + source, latest-vs-previous trend, last-5 history, web + native states. `getBodyWeightHistory` + `src/lib/weightDisplayCore.ts`.
- `a4556cd8` — live current-speed readout (mph) under the run pace hero via `currentSpeedMps` with 5 s freshness guard; verified with the simulated 5k source in the browser.
- Validation after all changes: npm run test 41 files / 378 tests PASS, lint PASS, build PASS, tsc PASS, `npx cap doctor` PASS, unsigned device-target Xcode build PASS. Browser-verified: auth page, /preview Dashboard + Fuel (consumed-energy RollingNumber hero), Settings Body weight block, live run screen with current speed.
- `.env.local` created locally (gitignored) with the Hyper-Dev URL + public anon key fetched via authenticated Supabase CLI; `node scripts/verify-ios-env.mjs` passes ("Hyper-Dev selected"). Supabase CLI in this repo remains linked to PRODUCTION ref nnwfaaxmyvqsdnfcdxom — never run db push from here without explicit re-verification.
- Not done / blockers: bundle-ID flip (awaits Account Holder verification of `app.hyper.mobile`), Hyper-Dev migration apply (awaits approval), photo-worker Hetzner deploy (awaits explicit infra approval), physical-device matrix + GPS field corpus (needs signing team + iPhones), TestFlight (Account Holder).

## Rev 70a — Phase 1 detail: recovery push and shell reconciliation

### Verified state

- Branch `codex/capacitor-ios-shell`, now tracking `origin/codex/capacitor-ios-shell`.
- `a17969d5` — safety checkpoint of the entire native parity batch (52 files, 2758 insertions), pushed to GitHub. Excludes `supabase/.temp/cli-latest` and `.claude/launch.json`; pre-commit sensitive-literal scan clean; validated with npm run test (40 files, 364 tests) PASS, lint PASS, build PASS.
- `e6cc3986` — merge of `origin/main` (through PR #58 merge `3bef3bb4`), pushed. Post-merge validation: npm run test (40 files, 366 tests) PASS, lint PASS, build PASS, no conflict markers.

### Merge resolution decisions

- ios/ shells: kept the local native shell wholesale (Hyper plugins, entitlements, deep-link AppDelegate, HyperBridgeViewController storyboard, permission strings, background location mode, portrait-only Info.plist). origin/main's ios/ was the bare generated Capacitor template; nothing of value was lost. `Package.resolved` is now tracked (from main) for reproducible SPM builds.
- Bundle ID: everything still uses `com.alexanderroesler.hyper` consistently (Xcode target, capacitor.config.ts, Info.plist URL scheme, native plugins, WHOOP allowlist). The flip to `app.hyper.mobile` is deliberately deferred to one scoped commit after the Account Holder verifies the registered explicit App ID (Rev 69 gate). origin/main's `app.hyper.mobile` values in capacitor.config.ts/pbxproj were NOT taken.
- `src/App.tsx`: main's design-elevation skeleton (BootSplash, AnimatedOutlet page turns, MotionConfig reducedMotion, PaperAtmosphere) + local RunTracker `/train/run` route, `/sandbox` dev route, native hooks (useNativeHealthSync/useWhoopForegroundSync/useNativeAuthCallback), and the fixed-viewport iOS clipped-scroll workaround. AnimatedOutlet scroll-to-top now targets `[data-app-scroll-viewport]` (falls back to window).
- `src/pages/Nutrition.tsx`: local consumed-energy hero semantics (`dayTotals.calories`) rendered with main's `RollingNumber`; dropped main's unused `remainingKcal` presentation and unused icon imports.
- `package.json`: `@capacitor/cli` kept in devDependencies; local ios:*/photo-worker scripts and `barcode-detector` retained. package-lock reconciled via npm install (no drift).
- `CLAUDE.md`: accepted main's untracking, added to .gitignore by main; file restored locally from `a17969d5` so local guidance is preserved (untracked/ignored). `.tmp/` ignore retained.
- Auto-merges verified to keep both sides: RestTimerPill (native notification sync + RollingNumber + pull-to-dismiss), appStore (connectWhoop returnTo), animations (pageTransition).

### Next actions

- Account Holder must verify explicit App ID `app.hyper.mobile` in Certificates, Identifiers & Profiles and its selection in the App Store Connect app record before the bundle-ID flip commit.
- Continue Rev 69 Phase 2/3: Hyper-Dev client config (.env.local), `npm run ios:sync`, Xcode Apple-framework build (Xcode 26.6 installed, license accepted — xcodebuild runs), physical-device checks.

## Rev 69 — canonical iOS ID and TestFlight-to-unlisted release gates

### User decision and verified constraints

- The intended canonical iOS bundle identifier is `app.hyper.mobile`. Treat this as the chosen identifier only after the Apple Account Holder verifies that the exact explicit App ID exists in Certificates, Identifiers & Profiles and is selectable in the matching App Store Connect app record. The current local branch still uses `com.alexanderroesler.hyper` in `capacitor.config.ts`, the Xcode target, native auth plugin, native bridge, WHOOP return allowlist, `Info.plist`, and URL schemes. Do not rewrite those identifiers piecemeal or register a second production App ID.
- The Apple membership belongs to the user's friend and is an Individual membership. Apple currently states that users invited by an Individual Account Holder receive App Store Connect access only and are not Apple Developer Program team members. The owner therefore remains responsible for the registered App ID, production signing/certificates, agreements, final archive/upload, App Review submission, and unlisted-distribution request. The user can be an internal TestFlight tester and can manage only what the granted App Store Connect role permits. Do not share Apple Account credentials or private signing keys casually.
- Food-database optimization is deferred. The build only needs the existing free barcode/search path and provider-neutral seams to work correctly for acceptance. Do not delay the first TestFlight build for Nutritionix, FatSecret approval, catalog benchmarking, or a speculative provider reorder. FatSecret/Nutritionix can be added later after written terms and credentials exist.
- Apple currently allows a TestFlight build to be tested for 90 days. Unlisted distribution is not a beta channel: Apple requires the app to be final, submitted to App Review, and identified in Review Notes as intended for unlisted distribution before the separate request is made. An unlisted link can be used by anyone who obtains it, so Hyper's server-side two-user allowlist remains required.
- Release is a separate guarded phase. Fable must finish a Hyper-Dev/TestFlight candidate and stop for the owner's explicit acceptance. It must not merge to production, change production Supabase/OAuth, deploy production services, submit App Review, request unlisted distribution, or release the app until the owner says exactly that the acceptance build passed and authorizes release.

### Fable 5 execution prompt

```text
Continue the hyPer iPhone app work in /Users/alex/Desktop/hyPer-current-ui. Do the implementation and verification, not merely a plan. Read AGENTS.md, TOOL_SWITCHING_CHECKLIST.md, and handoff.md Rev 69, Rev 68, and Rev 67 before acting. Keep handoff.md current after every material action.

MISSION
Produce one fully integrated, signed, physical-iPhone TestFlight acceptance build for the two intended users using Hyper-Dev. After the owner completes the acceptance matrix and explicitly says “GO FOR UNLISTED RELEASE,” prepare and submit the final production build for unlisted App Store distribution. Do not cross that release gate early.

NON-NEGOTIABLE SAFETY
- Preserve the dirty worktree. Do not reset, rebase, regenerate ios/ wholesale, or overwrite either Capacitor implementation.
- Do not touch /Users/alex/Desktop/hyPer, production main, production Supabase ref nnwfaaxmyvqsdnfcdxom, production OAuth, production hosting, production user data, the Hetzner VM, DNS, or external accounts without explicit approval for that exact action.
- Preserve and exclude supabase/.temp/cli-latest and .claude/launch.json from commits. Never stage .env files, credentials, cookies, provider sessions, signing keys, diagnostic routes, or raw private data.
- Use the existing package manager. Run targeted tests and then npm run test, npm run lint, npm run build, native checks, an actual Xcode Apple-framework build, and physical-device checks. Do not claim device behavior from syntax parsing or a simulator.
- The Apple membership is an Individual account owned by the user's friend. Do not ask for or share the friend's Apple password. Clearly label every step only the Account Holder can perform and provide exact commands/click paths for that person. The user may receive App Store Connect and internal TestFlight access but is not thereby an Apple Developer Program team member.

PHASE 1: RECOVERABLE REPOSITORY INTEGRATION
1. Verify branch, status, log, remotes, origin/main, the complete dirty diff, and current GitHub PR state.
2. Review the local native parity batch for correctness and secrets. Create and push a scoped safety checkpoint on codex/capacitor-ios-shell, excluding unrelated files and secrets.
3. Merge origin/main into the feature branch with a normal merge commit. Reconcile the two Capacitor SPM shells intentionally. Preserve main's current FOLIO/design-elevation UI and web behavior plus the local native bridges and Hyper-Dev production guard. Do not replace one ios/ directory wholesale with the other.
4. Verify with the Account Holder that `app.hyper.mobile` is the exact registered explicit App ID and the Bundle ID selected by the App Store Connect record. Once verified, make it canonical everywhere relevant: Capacitor app ID, Xcode target, provisioning/signing target, entitlements/application identifiers, native callback validation, URL types/schemes, TypeScript native bridge, Supabase native redirect allowlists, WHOOP native return allowlist, tests, and documentation. Use one native callback contract consistently. Preserve production web Google OAuth behavior.
5. Commit and push the reconciled integration branch. Report commit IDs and any unresolved conflicts.

PHASE 2: COMPLETE THE TESTABLE IPHONE PRODUCT
Finish or fix the following against Hyper-Dev only. Reuse the existing architecture and tests; do not redesign stable FOLIO typography, color, or navigation.

Authentication and session:
- Email/password, Google native OAuth, Sign in with Apple, callback/deep-link handling, Keychain-backed session restore, sign-out, expired-session behavior, and two-account server allowlisting.
- Keep browser auth working. Configure only the exact Hyper-Dev native callbacks first. Never point the native acceptance build at production to bypass an auth error.

Running and workouts:
- Long Run and Splits are the only startable running modes; legacy sprint records remain readable.
- Native Core Location/Core Motion recording must survive backgrounding and phone lock, restore interrupted sessions, reject bad samples, auto-pause/resume correctly, and save idempotently.
- Show live speed, rolling/current pace, average pace/speed, distance, elapsed/active time, GPS quality, and automatic/manual split results clearly. Preserve diagnostic raw-trace export locally and never upload coordinates by default.
- Finish optional user-authorized HealthKit workout/route export only if it can be done cleanly without blocking the core recorder. Do not claim Strava parity until the field corpus passes.
- Workout start/log/complete, set editing, past-workout editing, program view/edit/delete, volume status, and background rest-timer notifications must all work.

Nutrition:
- Manual foods, saved foods/meals create/edit/delete, immutable logged snapshots, meal buckets/order, daily targets/totals, editable macros, source badges, researched-description review, color photo capture/review, duplicate-safe retry, and barcode scan must work end to end.
- For this first build, retain a simple provider-neutral free lookup path with saved/personal exact barcode first and the currently functional Open Food Facts/USDA fallbacks. A catalog miss must have an editable manual/label-capture escape hatch and be saveable for the owner. Do not wait for FatSecret or Nutritionix and do not expose provider secrets in the app.
- Preserve explicit provenance and user review. Never merge nutrient fields from different providers or present AI estimates as verified database values.

Photo worker:
- Review the existing hardened worker and per-user routing. After explicit infrastructure approval, deploy the reviewed commit to the Hetzner CPX21 as an unprivileged supervised service behind private TLS ingress.
- Authenticate Supabase JWTs, enforce the exact two-user allowlist, isolate each user's permitted OpenAI/Codex and Claude subscription session, cap concurrency for CPX21, delete temporary images, redact logs, and verify restart/reboot, timeouts, expired auth, allowed/denied users, and duplicate-safe requests.
- Consumer-subscription automation is a private best-effort bridge, not an App Store entitlement or guaranteed provider API. Never bundle provider credentials in iOS. If reliable unattended use cannot be verified, surface a truthful unavailable/re-auth state and record the blocker.

WHOOP and weight:
- WHOOP connect, reconnect, foreground/app-open auto-sync, manual sync progress/result, idempotent imports, notes, durable owner-scoped deletion suppression/tombstones, and errors must work with both real accounts where applicable.
- Preserve EufyLife -> Apple Health -> Hyper. Request HealthKit body-mass access only when the user opts in, reconcile on foreground, dedupe by HealthKit UUID, and show latest weight plus simple history/trend, units, timestamp, source, empty/denied/error states. Validate with a real main-user Eufy weigh-in and a non-Eufy Health sample.

Native/UI quality:
- Fix iPhone safe-area opacity at the home indicator and sheet action areas, horizontal overflow, clipped tabs, 44-point targets, keyboard/focus behavior, loading/error/empty states, VoiceOver labels, Dynamic Type at practical accessibility sizes, dark/light appearance, and Reduce Motion.
- Preserve offline/retry safety and never show a “saved” state before durable persistence succeeds.

PHASE 3: HYPER-DEV AND DEVICE VERIFICATION
1. Apply only reviewed local migrations/functions needed by the app to Hyper-Dev after explicit approval. Record exact migration/function versions and rollback steps. Do not copy production secrets into the repository.
2. Compile and run the native app on a physical iPhone. Exercise denied/limited/granted permissions and cold/warm relaunches. Capture reproducible defects and add regression tests before fixes where practical.
3. Build the GPS field corpus: 10 minutes stationary outdoors; measured 400 m; known 1 km; 3-5 km with turns/trees; manual pause/resume; auto-pause; manual and automatic intervals; screen locked/backgrounded; interrupted/relaunched session; and matched Strava trials on the same routes. Compare distance error, split error, pace stability, callback gaps, and battery use. Tune only from evidence and retain rollbackable constants.
4. Run the complete two-account acceptance matrix for auth/session, Today, Train, Fuel, You, all must-work product flows, real WHOOP, real Eufy/HealthKit, both photo providers, barcode hits/misses, saved/manual foods, offline/retry, and accessibility.
5. Run npm run test, npm run lint, npm run build, git diff --check, Capacitor sync/doctor, Xcode build/archive validation, and a sensitive-file scan. All required checks must pass or be reported as a named blocker.

PHASE 4: TESTFLIGHT ACCEPTANCE BUILD
1. Have the Account Holder create/verify the App Store Connect app record for `app.hyper.mobile`, accept current agreements, configure signing/capabilities, archive the release candidate, and upload it. Use a normal TestFlight-capable build, not “TestFlight Internal Only,” because the accepted binary may later become the release candidate.
2. Create an internal TestFlight group for the two users, add the build, provide concise What to Test notes, install on both real iPhones, and verify crash/feedback collection. Remember each TestFlight build expires after 90 days.
3. Deliver a numbered acceptance checklist with expected results and a defect template. Keep this build on Hyper-Dev and keep production untouched.
4. HARD STOP. Wait for the owner to state that all release-blocking tests passed and explicitly say “GO FOR UNLISTED RELEASE.” A request to fix a test defect is not release approval.

PHASE 5: ONLY AFTER EXPLICIT RELEASE APPROVAL
1. Review every accepted diff again. Create a scoped release PR into production main, obtain the requested independent review, resolve findings, and rerun all checks. Promote database migrations, Edge Functions, OAuth/native callbacks, hosting config, WHOOP config, and VM routing to production one subsystem at a time with backups, rollback steps, and smoke tests. Never replace working production web Google OAuth; extend it for native callbacks.
2. Build a production-signed archive from the reviewed commit with a unique monotonically increasing version/build number. Verify it points only to production public client configuration and that secrets remain server-side.
3. Complete App Store metadata and review readiness: final name/subtitle/description/category/age rating, real iPhone screenshots, app icon, support URL, accessible in-app and metadata privacy-policy URL, App Privacy answers, export-compliance answers, permission-purpose strings, in-app account deletion, Sign in with Apple equivalence for Google login, reviewer contact, an active allowlisted review account or fully functional review mode, and detailed notes for GPS/background location, HealthKit, camera/photos, WHOOP, and the private AI worker. The backend and review account must remain available throughout review.
4. Select manual release control. Add a Review Notes statement that the app is intended for unlisted distribution, submit the final app version to App Review, and then submit Apple's separate Unlisted App Distribution request. Do not make the app publicly searchable and do not click final release before Apple approves the unlisted request/classification.
5. If Apple rejects either the app or unlisted request, do not work around the review. Record the exact reason, make the smallest compliant fix, retest, and resubmit.
6. After approval, have the Account Holder release the approved unlisted version, verify the direct App Store link on both intended devices, confirm the app does not appear in search, and run production smoke tests for auth, sync, GPS, photo, barcode, WHOOP, and HealthKit. Keep the server-side two-user allowlist because anyone with the unlisted link can download the app.
7. Tag the exact release commit, record build/version, migrations/functions, deployment artifacts, App Store link, rollback/incident steps, and final verification in handoff.md without secrets.

DONE MEANS
- Before the hard stop: a reproducible TestFlight build installed on both iPhones, connected only to Hyper-Dev, with the full acceptance checklist ready and no unreported P0/P1 issue.
- After explicit release approval: an App-Review-approved, Apple-approved unlisted App Store release from the exact reviewed commit, with production smoke tests passed and the two-user access boundary enforced.
```

## Rev 68 — remaining GPS, food, VM, WHOOP, and Eufy work

### Current quality assessment

- The local architecture is a strong pre-Xcode foundation, not a finished or Strava-validated iOS app. Automated validation remains green from Rev 66, but the Swift plugins have only passed syntax parsing, not an Xcode Apple-framework build, signing, background runtime, or physical-device acceptance. Resolve Rev 67's branch/Capacitor/bundle-ID conflict before feature expansion.
- The native run plugin already requests best-for-navigation accuracy, declares fitness activity, disables automatic Core Location pauses, uses no distance filter, creates an iOS 17 background activity session, records Core Motion state, persists every raw `CLLocation` to a protected JSONL trace, restores interrupted recording, and feeds the existing tested pace/split/auto-pause reducer. This is the correct foundation, but code review cannot establish Strava parity or superiority.
- Remaining run work is: Xcode compile/runtime fixes; cold-launch/background/locked-screen/termination recovery tests; optional HealthKit `HKWorkout` plus `HKWorkoutRouteBuilder` export (currently absent despite the permission copy); native background audio/haptic split cues if desired; a post-run replay/reprocessing path that preserves the raw trace and compares candidate filters without silently rewriting history; current-speed plus rolling/average pace presentation; battery/cadence diagnostics; and a measured outdoor calibration corpus. Tune only after known-distance and matched-Strava evidence exists.
- The photo worker is materially hardened and deletes each temporary image directory in `finally`. It is not deployed. Remaining work is operational: reviewed-code transfer, Linux Node/CLI compatibility, an unprivileged service user, provider authentication, private ingress/TLS, Supabase JWT plus explicit user allowlist, systemd/reboot verification, auth-expiry monitoring, and failure/queue/restart tests. One process has one Codex and Claude identity; two consumer-subscription users require isolated OS users/worker instances plus authenticated user-to-instance routing. Do not pool one subscription login.
- WHOOP foreground/app-open reconciliation exists, but there is no webhook receiver and no durable user-deletion suppression. A WHOOP walk deleted from Hyper can therefore reappear on a later import. Add an owner-scoped provider tombstone/suppression mechanism before presenting delete as permanent, then consider signed/idempotent webhooks plus periodic reconciliation.

### Barcode and database decision

- Zero-cost optical decoding is already strong: native iOS uses VisionKit/DataScanner and the web fallback uses ZXing. Catalog coverage is a separate problem. Current live free lookup is USDA Branded first, then Open Food Facts; FatSecret has only a response mapper and is not activated. No free, approval-free database was found that can honestly be claimed to match Cronometer's combined curated/provider coverage.
- Best available no-approval stack is: the user's own saved barcode catalog first, Open Food Facts exact-barcode data, then USDA Branded, followed by a nutrition-label/front-package capture flow that creates an editable saved personal product bound to that barcode. This makes an initial miss reusable on every future scan. Open Food Facts is free/open but community-contributed and explicitly provides no accuracy/completeness guarantee; retain provenance, attribution/license compliance, and user review.
- Current code does not query a user's saved foods by barcode before external providers and does not offer label OCR/capture after a miss. It also uses deprecated Open Food Facts API v2 even though v3 is recommended for new integrations. These are the highest-value free barcode code improvements. Benchmark optical decode and catalog match separately on 50–100 products before changing provider priority or making quality claims.
- FatSecret Premier Free remains the strongest pending free provider candidate for this student project. Its current official page says students/student research groups qualify, includes US barcode scanning/autocomplete/caching with required attribution, and publishes vendor claims of 90%+ global UPC/EAN coverage, verified items, and zero duplicates. Treat those as vendor claims until Hyper's personal benchmark confirms them. Keep its client secret and token request on a static-egress server; do not ship credentials to iOS.
- Nutritionix Business Trial can still be attempted honestly as a qualified student research evaluation with two MAUs. Its current request page says it does not offer open public access for personal/non-commercial/student use but does offer limited free trials for qualified commercial, research, and enterprise evaluations. Apply using a real university `.edu` address if available, describe the independent student research and two-user evaluation, and do not invent a company or affiliation. Approval, duration, quota, production rights, and the no-general-caching restriction remain external gates; it is not a permanent free plan.

### EufyLife through Apple Health

- Eufy's current support documentation confirms EufyLife on iOS can connect to Apple Health and notes that only the main EufyLife user's data syncs; separate users should use separate EufyLife accounts. Hyper's native bridge already requests Apple Health body-mass read access, observes/background-delivers changes, reads sample UUID/time/kg/source, upserts idempotently with RLS, and reconciles on app open/foreground.
- The imported weight is currently visible only inside the Settings connection row. To satisfy “alongside everything else,” add a latest-weight surface and a simple weight-history/trend view in the existing FOLIO style, with lb/kg preference, measured time, source, empty/error/permission states, and no medical interpretation. Test EufyLife → Apple Health → Hyper with a real main-user weigh-in, duplicate/retry, offline foregrounding, permission denial, and a non-Eufy Apple Health weight. Do not claim Hyper can force EufyLife to upload; it can import only after EufyLife writes Apple Health.

### Next Fable 5 work package

```text
Continue in /Users/alex/Desktop/hyPer-current-ui on codex/capacitor-ios-shell.

Read AGENTS.md, TOOL_SWITCHING_CHECKLIST.md, and handoff.md Rev 68 then Rev 67. Preserve the dirty worktree and production boundary. Do not touch /Users/alex/Desktop/hyPer, production Supabase, production OAuth, production deployment, or main. Preserve supabase/.temp/cli-latest and .claude/launch.json.

Phase 1 is mandatory before new features: review the complete local native batch, rerun all required checks, create and push a safety checkpoint excluding unrelated files/secrets, merge origin/main into the feature branch, and intentionally reconcile the two Capacitor SPM projects. Preserve main's design-elevation UI and the local native plugins/Hyper-Dev build guard. Ask which Apple bundle ID is registered before resolving app.hyper.mobile versus com.alexanderroesler.hyper if it cannot be verified.

Phase 2: compile in Xcode against Hyper-Dev and fix all real Apple-framework errors. Add tests where practical. Run on a physical iPhone and validate native auth, Keychain restore, background GPS, barcode, notifications, and HealthKit. Do not claim success from Swift parse or simulator-only checks.

Phase 3 GPS: keep only Long Run and Splits startable while preserving legacy sprint records read-only. Finish robust background/locked-screen/interruption recovery, expose current speed plus rolling and average pace clearly, add optional user-authorized HKWorkout/HKWorkoutRoute export, and add privacy-preserving raw-trace replay/calibration tooling. Do not guess new filters or claim Strava parity until the 400 m, 1 km, 3–5 km, stationary, auto-pause, interval, and matched-Strava field corpus is collected. Separate measured code defects from calibration work.

Phase 4 food/barcodes: create a provider-neutral exact-barcode pipeline. Query an owner-scoped saved personal barcode first; then Open Food Facts; then USDA Branded while Nutritionix/FatSecret remain disabled adapters until credentials/approval exist. Upgrade Open Food Facts to its current supported API, retain required User-Agent/provenance, validate serving/macros, and handle provider timeout versus true not-found distinctly. On a total miss, offer front-package plus nutrition-label capture, editable OCR/AI review, and save the product to that user's barcode catalog so the next scan resolves locally. Never merge nutrient fields from different providers. Add regression tests and a 50–100 product benchmark harness/report.

Phase 5 Eufy/Health: keep the path EufyLife → Apple Health → Hyper. Surface latest weight and a simple trend/history in Today or You using the FOLIO design, with lb/kg, timestamp, source, empty/error/permission states, and no medical claims. Preserve UUID dedupe and app-open/foreground reconciliation. Validate with real Eufy main-user samples after the migration is applied to Hyper-Dev only.

Phase 6 WHOOP: add durable owner-scoped suppression/tombstones so a deleted imported walk or other WHOOP activity does not reappear on the next sync. Preserve user-authored notes and idempotency. Design signed/idempotent webhook ingestion plus periodic reconciliation, but do not deploy cloud changes without explicit approval.

Phase 7 VM: finish any missing per-user routing/configuration needed for isolated provider identities, then produce an exact Hetzner CPX21 deployment and rollback checklist. Do not SSH, install packages, change firewall/DNS/Tailscale, start services, or copy credentials without explicit approval. Once authorized, deploy only the reviewed checkpoint and verify health, allowed/denied users, both providers, queue saturation, restart/reboot, auth expiry, timeouts, and temporary-image deletion.

Run targeted tests throughout, then npm run test, npm run lint, npm run build, native structural checks, and Xcode/device checks. Update handoff.md after every material action with exact results and blockers. Stop before any production merge or deployment and provide the owner a concrete iPhone acceptance checklist.
```

## Rev 67 — GitHub Capacitor audit and Fable 5 transfer plan

### Verified remote state

- Repository is `staylan488-ux/hyPer`; default branch is `main`. A read-only GitHub/fetch audit on 2026-07-20 confirmed PR [#58](https://github.com/staylan488-ux/hyPer/pull/58), `feat(ios): add Capacitor iOS platform`, was merged into `main` at `3bef3bb400bc2909b37ae1d00efc527ade421dc1`. Its source commits are `d590a50e5d1cbcc50f0d1a167f548aef6b99ec20` and `a4ba0a069898da114263ffa512b24b0e0180b79a`; remote branch `feat/ios-capacitor` remains present.
- PR #58 contains a generated Capacitor 8 Swift Package Manager iOS shell, Xcode project/assets, `capacitor.config.ts`, and Capacitor package changes. Its configured bundle ID is `app.hyper.mobile`. The code and merge are verified on GitHub; the friend's report that it launches successfully as a standalone app was not independently reproduced in this checkout or on a device during this audit.
- Remote `main` also contains merged design-elevation PR #57. Relative to merge base `0da91009553dbd40e79ef3cfc1a4629de981d05f`, `origin/main` is 12 commits ahead and local `codex/capacitor-ios-shell` HEAD is 14 commits ahead. The local branch has no matching remote branch, and the current uncommitted native parity batch is not on GitHub.

### Verified local state

- Worktree remains `/Users/alex/Desktop/hyPer-current-ui` on `codex/capacitor-ios-shell`, committed HEAD `d356c516 feat(ios): scaffold Capacitor app shell`. It contains a separate Capacitor SPM project configured as `com.alexanderroesler.hyper`, plus an uncommitted native parity batch: native OAuth/Keychain/deep links, Core Location/Core Motion background run capture and recovery, HealthKit weight sync, VisionKit barcode scanning, local-notification rest timers, WHOOP lifecycle/native-return handling, worker hardening/deployment files, one local database migration, adapters, and regression tests.
- Latest completed validation for this exact native batch remains PASS: Swift syntax parse through Command Line Tools, Node syntax, plist/project/storyboard validation, `npm run lint`, `npm run test` (40 files, 364 tests), `npm run build`, `git diff --check`, `npx cap doctor`, and sensitive-literal scan. Full Xcode Apple-framework compile/sign/device validation remains outstanding.
- Preserve unrelated user-owned working-tree files `supabase/.temp/cli-latest` and `.claude/launch.json`; do not stage, discard, or overwrite them. No local native parity commit or push has been made. Production checkout `/Users/alex/Desktop/hyPer`, production Supabase ref `nnwfaaxmyvqsdnfcdxom`, Hyper-Dev cloud state, VM, provider credentials, and external accounts were not changed by this audit.

### Required reconciliation decision and safe order

- Do not checkout `main`, pull, reset, rebase, or regenerate `ios/` while this worktree is dirty. First review the local diff and create a safety commit on `codex/capacitor-ios-shell`, explicitly excluding the two unrelated files above and all secrets/generated junk. Push that branch before integration so the native batch has an off-Mac recovery point.
- Then merge current `origin/main` into the feature branch in a normal merge commit. Preserve main's design-elevation work and current web behavior while retaining the local native plugins and safety guard that locks native builds to Hyper-Dev. Resolve the two generated Capacitor projects intentionally; never copy one `ios/` tree over the other wholesale.
- Bundle IDs conflict: GitHub shell uses `app.hyper.mobile`; local native callbacks/entitlements use `com.alexanderroesler.hyper`. Before resolving Xcode project, URL scheme, entitlements, OAuth callbacks, and App Store Connect records, verify which App ID the Apple Account Holder actually registered. Use one canonical identifier everywhere. Do not guess or register a second production App ID.
- Remote PR #58 removed tracked `CLAUDE.md` as local-only. Preserve project instructions through `AGENTS.md`, this `handoff.md`, and `TOOL_SWITCHING_CHECKLIST.md`; do not let that deletion remove required local guidance during the merge.

### Remaining work for Fable 5

1. Inspect and safety-commit/push the current native batch without the unrelated user files or secrets.
2. Merge `origin/main`, reconcile the competing Capacitor shells and bundle-ID decision, preserve the design-elevation UI, then rerun tests/lint/build and native structural validation.
3. After Xcode license/components are complete, add ignored Hyper-Dev public client configuration, run `npm run ios:sync` and `npm run ios:open`, compile the Swift plugins, configure only the required capabilities, and run on a physical iPhone. Never substitute production Supabase credentials to bypass the environment guard.
4. In Hyper-Dev only, configure the exact final native OAuth callback, enable/verify Google and Apple providers, apply `20260719230000_add_body_weight_measurements.sql`, and deploy the changed WHOOP Edge Function only after explicit approval. Verify Google web auth remains unchanged in the production web app.
5. Execute device acceptance for cold/warm auth restore; Google/Apple callbacks; background/locked-screen long run and splits; interruption recovery; live/average/split pace; auto-pause; HealthKit/Eufy weight dedupe; barcode decode plus provider fallbacks; rest notifications; WHOOP connect/foreground reconciliation; safe-area UI; and all six must-work web product flows.
6. Build the measured GPS calibration corpus against known 400 m, 1 km, and 3–5 km routes plus matched Strava trials. Tune only from exported diagnostics; do not claim Strava parity before the documented accuracy/background gates pass.
7. Deploy the hardened photo worker to Hetzner only after explicit infrastructure approval, private TLS ingress, Supabase authentication plus user allowlist, separate permitted provider identities per tester, service supervision, retention/logging review, and a restart/auth-expiry test. Consumer-subscription inference remains a private best-effort bridge, not a guaranteed production entitlement.
8. FatSecret Premier Free is awaiting provider review. The truthful application position is a non-commercial student personal-research project with no funding or revenue. Do not add credentials or activate FatSecret until approval, terms, attribution/storage requirements, and the personal barcode benchmark are documented.
9. Do not merge or push this integration to production `main`, deploy production, change production OAuth, or apply production migrations until the full Hyper-Dev iPhone test matrix and independent review pass.

### Fable 5 startup instruction

```text
Continue the hyPer Capacitor/iOS integration in /Users/alex/Desktop/hyPer-current-ui.

First read AGENTS.md, TOOL_SWITCHING_CHECKLIST.md, and the top of handoff.md through Rev 67. Then verify branch, status, log, origin/main, and the complete local diff. Do not discard or overwrite the dirty working tree.

Your first deliverable is a reviewed safety commit and pushed recovery branch for the intended native parity batch. Exclude supabase/.temp/cli-latest, .claude/launch.json, every .env file, credentials, generated junk, and unrelated changes. Run the required test/lint/build and native structural checks before committing.

Next merge origin/main into codex/capacitor-ios-shell and reconcile the two Capacitor SPM shells. Preserve origin/main's design-elevation UI and web behavior, preserve the local native plugins and Hyper-Dev safety guard, and do not regenerate ios/ over either implementation. Stop and ask which bundle ID the Apple Account Holder actually registered before resolving app.hyper.mobile versus com.alexanderroesler.hyper if that cannot be verified locally.

After the merge, compile in Xcode against Hyper-Dev on a physical iPhone and finish the Rev 67 device/deployment checklist. Do not touch production Supabase, production OAuth, production deployment, or production main without explicit approval and completed Hyper-Dev acceptance. Update handoff.md after every material action and report exact commands/results, remaining blockers, and the next safe action.
```

## Rev 66 — pre-Xcode native parity foundation implemented

- Branch remains `codex/capacitor-ios-shell`. Production web code at `/Users/alex/Desktop/hyPer`, production Supabase ref `nnwfaaxmyvqsdnfcdxom`, cloud configuration, VM, and external accounts were not changed. Browser Google OAuth retains its existing web redirect behavior; all new native behavior is gated by Capacitor iOS detection and is intended for isolated Hyper-Dev ref `nwvgkxqjqihqnjuworqz` first.
- Added native iOS bridges and Swift implementations for PKCE OAuth through `ASWebAuthenticationSession`, native-only Sign in with Apple, Keychain Supabase session storage, strict custom-scheme callbacks, background Core Location/Core Motion run capture with protected JSONL trace recovery, VisionKit barcode scanning, local-notification rest timers, and HealthKit body-weight reads/background reconciliation. The existing TypeScript run reducer continues to own filtering, active time, pace, auto-pause, and split interpolation while native recording supplies durable background samples.
- Added authenticated HealthKit weight upsert logic plus `supabase/migrations/20260719230000_add_body_weight_measurements.sql`; native Settings controls are opt-in. Added foreground WHOOP reconciliation and a signed/allowlisted native WHOOP OAuth return route. These schema and Edge Function changes are local only and have not been applied or deployed to Hyper-Dev or production.
- Hardened the private photo-food worker with Supabase authentication timeouts, explicit user allowlisting, bounded concurrency/queueing, stable idempotency keys and response caching, provider-status caching, command timeouts, graceful shutdown, redacted client errors, and systemd/example deployment files under `deploy/photo-worker/`. The worker was not deployed or started on Hetzner. Each tester still requires a separate permitted provider identity/worker instance if consumer subscription authentication is used; one shared CLI login must not be pooled.
- Added native/web adapters and regression coverage for OAuth parsing, secure timer behavior, weight normalization, WHOOP sync throttling, request idempotency, and worker queue/allowlist behavior. Final validation PASS: Swift source parse via Command Line Tools, Node syntax checks, `plutil -lint`, storyboard XML validation, `npm run lint`, `npm run test` (40 files, 364 tests), `npm run build`, `git diff --check`, and `npx cap doctor` (`iOS looking great`). Existing large JavaScript chunk and stale Browserslist warnings remain non-blocking.
- Full Apple-framework compilation, signing, capabilities, background execution, HealthKit, VisionKit, OAuth, notification delivery, and GPS quality are not yet verified. Xcode was installed/selected during the session but its license has not been accepted, and that is a user/system action. `npm run ios:sync` also intentionally refuses to run until ignored `.env.local` contains the Hyper-Dev URL and public anon/client key; do not bypass this guard or use production credentials.
- Preserve unrelated user files `supabase/.temp/cli-latest` and `.claude/launch.json`. No commit was requested or created. Exact next action: accept/open Xcode and install its components, add the ignored Hyper-Dev public client configuration, run `npm run ios:sync` then `npm run ios:open`, choose the friend's signing team and a physical iPhone, compile, fix any Apple API/compiler findings, add the exact native callback URL to Hyper-Dev only, then perform the documented device acceptance tests before any production merge or configuration change.

## Rev 65 — native/server parity gap and production-auth boundary

- Production remains `/Users/alex/Desktop/hyPer` with a working Google web sign-in flow and production Supabase ref `nnwfaaxmyvqsdnfcdxom`. Treat that implementation as the regression reference and do not copy, replace, or reconfigure its OAuth/provider settings while developing the native client. The Capacitor branch remains isolated against Hyper-Dev ref `nwvgkxqjqihqnjuworqz`; eventual delivery is an intentional reviewed code merge plus separately verified native redirect configuration, not a staging-to-production configuration overwrite.
- The checked-in Capacitor shell does not yet provide the target native capabilities. Remaining priority code is: native OAuth callbacks/deep links plus Sign in with Apple and secure session storage; a Swift Core Location/Core Motion Capacitor plugin with background/locked-screen recording, durable trace recovery, live/average pace, auto-pause, exact manual/automatic split interpolation, diagnostics, and field calibration; native camera/barcode scanning; local-notification rest timers; HealthKit import/export; app lifecycle/offline synchronization; and production privacy/account-deletion surfaces.
- The existing photo worker is a development bridge, not a 24/7 service. Before VM deployment it needs Linux CLI/runtime validation, explicit two-user allowlisting in addition to Supabase authentication, isolated provider identities, bounded queue/concurrency, idempotent jobs, retries/timeouts/cancellation, restart supervision, auth-expiry monitoring, private TLS ingress, short-lived encrypted image handling, structured output validation, and an official API fallback. A Hetzner CPX21 is sufficient to orchestrate remote frontier inference but cannot self-host a frontier vision model. OpenAI documents API-key auth as the default for programmatic automation and warns against exposing Codex execution to untrusted/public environments; saved subscription authentication remains a private, revocable beta bridge rather than a guaranteed production entitlement.
- Current Eufy documentation confirms that EufyLife on iOS can sync a paired main user's scale data to Apple Health. Native Hyper can implement an initial HealthKit body-mass import plus anchored/observer updates, UUID/source dedupe, unit normalization, and foreground reconciliation. This is automatic after EufyLife writes the HealthKit sample, but Hyper cannot force the scale/EufyLife sync or rely on an undocumented Eufy cloud API.
- WHOOP server OAuth/import behavior, existing nutrition/manual/saved-meal logic, and the TypeScript run engine can be reused. Native lifecycle sync, webhook reconciliation, barcode-provider approvals/benchmarks, secure offline persistence, and real-device acceptance still remain. A Strava-comparable recorder is a measurable calibration target, not a universal accuracy guarantee because phone GNSS and environmental limits remain shared constraints.
- Exact next action: install/open Xcode 26+ on the signing owner's Mac, build the current Hyper-Dev Capacitor shell on a physical iPhone, verify layout/session persistence without touching production, then implement native auth parity first and the background run plugin second. No source, schema, dependency, VM, cloud configuration, credential, deployment, production system, or external account changed during this assessment; only this handoff research/plan note changed.

## Rev 64 — friend-owned individual Apple account workflow

- The Apple Developer Program membership belongs to the user's friend and is an individual membership. Apple currently allows an individual Account Holder to invite up to 50 additional App Store Connect users, but those users are not members of the Developer Program team and cannot access Certificates, Identifiers & Profiles. Therefore GitHub, Supabase, or LLM collaborator access does not grant iOS signing authority, and the friend's Apple Account/password/2FA must not be shared.
- Clean workflow: friend installs Xcode 26+, signs in with the enrolled Apple Account, clones/checks out `codex/capacitor-ios-shell`, receives Hyper-Dev client configuration through an ignored local environment file or direct Supabase collaboration, runs the checked-in Capacitor sync, selects the friend's signing team, registers the final bundle ID, builds on a physical iPhone, and uploads signed builds. Friend then invites the user to App Store Connect with Developer or App Manager access and distributes through an internal TestFlight group; Apple permits up to 100 internal testers and each build lasts 90 days.
- Ownership consequence: an individual membership publishes under the friend's legal name, the friend remains the sole Account Holder responsible for agreements/renewal/certificates, and transfer of an individual Account Holder role is limited and requires Apple assistance. Confirm this is acceptable before registering the final App ID or creating the production App Store record. For initial Hyper-Dev device/TestFlight testing it is workable. Production, App Store Connect, signing resources, cloud configuration, and credentials were not changed during this research.

## Rev 63 — Capacitor-first iOS foundation

- User chose an incremental Capacitor migration before a full Swift rewrite and confirmed an individual Apple Developer account. Existing GPS work was validated (`npm run test`: 347/347, `npm run lint`: PASS, `npm run build`: PASS) and checkpointed as `5d899809 feat(run): harden web GPS tracking`; Capacitor work is isolated on `codex/capacitor-ios-shell`.
- Added official Capacitor `8.4.2` core/CLI/iOS packages and generated the Swift Package Manager iOS project in `ios/`. Local product identity is `hyPer`, bundle ID `com.alexanderroesler.hyper`, iOS 15+, iPhone-only, portrait-only. Capacitor uses bundled `dist` assets; the existing hosted PWA remains a separate web build and the service worker is not registered inside the native runtime.
- Added explicit camera, photo-library, foreground/background-run location, and motion permission descriptions. Capacitor is configured edge-to-edge with CSS safe-area handling, a paper-color WebView background, mobile content mode, no link previews, and debug-only native logging.
- Added `build:ios`, `ios:sync`, `ios:open`, and `ios:run` scripts. `scripts/verify-ios-env.mjs` refuses native builds unless the configured Supabase hostname is isolated Hyper-Dev ref `nwvgkxqjqihqnjuworqz` and a public client key is present. Missing environment rejection PASS; a non-secret dummy Hyper-Dev-shaped build plus `cap sync ios` PASS; `plutil -lint` PASS; `npx cap doctor` reports Capacitor iOS healthy.
- Full Xcode remains absent: `xcode-select` still points to Command Line Tools and `xcodebuild` cannot run. Capacitor 8 officially requires Xcode 26+ and iOS 15+. Install/open Xcode 26+, accept its setup, then add an ignored `.env.local` containing the Hyper-Dev URL and public/anon key, run `npm run ios:sync`, `npm run ios:open`, select the individual signing team and a physical iPhone, and build. Do not register or configure production yet.
- First-device shell acceptance precedes native bridges. Then implement native OAuth/deep-link handling plus Sign in with Apple, followed by a custom Swift Core Location/Core Motion Capacitor plugin for locked-screen/background Long Run and Splits. The official Capacitor geolocation plugin explicitly does not support background geolocation directly, so it is not sufficient for the target recorder. Continue with native camera/barcode, local notifications/rest timers, HealthKit, and other capabilities one adapter at a time; only begin the full SwiftUI refactor after measured Capacitor parity. The separate `/Users/alex/Desktop/hyPer-ios` prototype is retained, paused, and not deleted.
- Production repository, production Supabase, Hyper-Dev cloud configuration/data, VM, credentials, and external accounts were not changed. Preserve unrelated user files `supabase/.temp/cli-latest` and `.claude/launch.json`.

## Rev 62 — isolated native foundation started and paused

- Created a separate repository at `/Users/alex/Desktop/hyPer-ios` on branch `codex/native-foundation`; the production web clone, production Supabase, Hyper-Dev, VM, credentials, and external accounts were not changed. Pure Swift run core and atomic local snapshot code were added; `swift build --target HyperRunCore` and six deterministic executable checks passed.
- Full Xcode is not installed/selected, so signing, simulator/device builds, and XCTest are blocked. A Core Location/Core Motion adapter was patched immediately before the user paused work and remains uncompiled/unvalidated. Resume from the native repository's `handoff.md`; first install/open full Xcode, select it, compile the adapter, and rerun all checks. No production mutation occurred.

## Rev 61 — full-run GPS callback gap reported

- User reports every newly finished real-phone run shows `Longest gap` approximately equal to the run duration. Source inspection confirms `summarizeGpsTrace` measures the maximum timestamp difference between every consecutive raw trace observation, including warming, stationary, rejected, and accepted samples; it is not incorrectly measuring only accepted distance points.
- The observed display therefore means the run trace contains only one/two widely separated geolocation callbacks or Safari supplied anomalous timestamps. Motion denial cannot directly create this metric because motion only annotates a location callback already received. Exact diagnosis requires the exported private GPS JSON, especially `summary.quality.sampleCount` and the trace timestamps/decisions. The current warning copy is too specific when it says Safari definitely paused; callback starvation can also come from screen locking/background suspension, weak/indoor positioning, or browser/OS throttling. No source fix was authorized or made; only this handoff diagnosis changed.

## Rev 60 — cached Safari motion denial diagnosed

- Deleting the Hyper Tailscale site's Safari website data did not restore the Motion & Orientation prompt. Source inspection reconfirmed the request is made synchronously in the Run Start tap chain (`handleStart` → tracker `start` → source `start` → `DeviceMotionEvent.requestPermission()`), so the required user activation is present; Safari is returning its previously cached denial without prompting.
- Next least-destructive recovery is Settings → Apps → Safari → Clear History and Website Data for all history/profiles, restart the iPhone, reopen the exact HTTPS URL in full Safari, and tap Run → Start. If that still fails, Apple's system-wide Settings → General → Transfer or Reset iPhone → Reset → Reset Location & Privacy is the final supported permission reset, but it makes all apps ask for protected-resource permissions again. No source, schema, dependency, configuration, deployment, production system, external account, or credential changed; only this handoff diagnosis changed.

## Rev 59 — iPhone motion permission recovery

- During the real iPhone web GPS test, the user declined Safari's Motion & Orientation prompt once and Safari did not prompt again. Current source requests `DeviceMotionEvent.requestPermission()` synchronously from the Run Start gesture and intentionally continues with GPS when denied; there is no Hyper-local denial flag to clear.
- Recovery is an iOS/Safari site-permission reset: first save/discard any active run, then delete only the `alexanders-macbook-air.taileaf222.ts.net` entry under Settings → Apps → Safari → Advanced → Website Data, ensure Safari's Motion & Orientation Access is enabled if that control is present, close/reopen Safari, load the exact HTTPS site, and tap Run → Start → Allow. Deleting site data can remove the Hyper web login and local interrupted-run recovery. No source, schema, dependency, configuration, deployment, production system, external account, or credential changed; only this handoff test note changed.

## Rev 58 — production delivery sequence revalidated

- Production sequence remains: field-test and checkpoint the current web beta; enroll the individual Apple Developer account and lock product identity; build a separate SwiftUI client against Hyper-Dev; prove native background Long Run/Splits and HealthKit routes; port nutrition, WHOOP, workouts, timers, and account management; harden the private worker/provider boundary; complete two-user TestFlight; then submit the final build to App Review and request unlisted distribution.
- Current official boundaries checked on 2026-07-19: Apple requires a final, non-beta build already submitted to App Review before approving unlisted distribution, and anyone with the link can download it, so Hyper must retain its two-account server allowlist. Google primary login requires an equivalent privacy-preserving login option and account creation requires in-app deletion. App Store privacy declarations must include third-party handling of health, fitness, location, photos, and identifiers.
- Native implementation should retain Supabase for auth/data/functions, add native Sign in with Apple plus existing Google with identity-linking tests, and keep Hyper-Dev and production configurations physically separate. WHOOP remains server-side OAuth with refresh/offline access and webhooks. FatSecret Premier Free remains approval-gated and requires its client secret and token exchange off-device; its documented IP allowlisting may make the Hetzner static-egress worker useful for FatSecret as well as photo inference.
- OpenAI currently documents ChatGPT sign-in for local Codex subscription access, but recommends API-key authentication for programmatic Codex CLI workflows and restricts trusted subscription-backed automation tokens to eligible Enterprise workspaces. Anthropic likewise states consumer subscriptions and API product access are separate. Therefore a consumer-subscription VM can remain a private two-user bridge only if each provider/account permits it and each user has an isolated identity; production reliability requires a budget-capped official API fallback or acceptance that photo logging can become unavailable when login, limits, or terms change.
- Exact next action remains to complete the outdoor web GPS matrix and export diagnostics, fix only measured blockers, run `npm run test`, `npm run lint`, and `npm run build`, review and checkpoint the dirty branch, then begin Apple enrollment/product-identity inputs and the native specification. No source, schema, dependency, VM, cloud configuration, credential, deployment, production system, external account, purchase, form submission, or provider configuration changed; only this handoff plan/research note changed.

## Rev 57 — native hosting boundary clarified

- The existing production repository is a Vite/React PWA backed by Supabase, but contains no committed Vercel, Netlify, Cloudflare, Firebase, GitHub Pages, or other hosting configuration, so the exact current static-host provider was not verifiable from either local clone. Preserve that web deployment as the production web version, regression reference, and host for public support/privacy/terms and OAuth/universal-link pages.
- The native iOS UI will not be served from the current web host. Apple will distribute the signed client through TestFlight and ultimately the unlisted App Store; it can continue using the existing production Supabase backend after isolated Hyper-Dev validation and an intentional promotion. The private photo-inference worker is a separate Hetzner service. The current Tailscale URLs are development-only and are not the future public production host. No source, schema, dependency, VM, cloud configuration, credential, deployment, production system, or external account changed; only this handoff clarification changed.

## Rev 56 — device-test services and foundation choices

- User chose Apple Developer Program individual enrollment and will run the current outdoor GPS test. Seller-name implications remain accepted/pending enrollment; no Apple account or purchase was changed in this session.
- Planned inference host is a Hetzner CPX21. It is adequate for the current Codex/Claude CLI worker because inference runs remotely, but should not be treated as capable of self-hosting a frontier multimodal model. Exact OS/network hardening and migration remain pending.
- Verified the current Mac test stack on 2026-07-19: app process is listening on `127.0.0.1:5173`, worker on `127.0.0.1:8788`, both local health checks return HTTP 200, and private Tailscale Serve routes return HTTP 200. Phone app URL is `https://alexanders-macbook-air.taileaf222.ts.net/`; worker URL is `https://alexanders-macbook-air.taileaf222.ts.net:8443`. Both routes are tailnet-only. Keep Tailscale connected and the existing processes running for the field test. No source, schema, dependency, VM, cloud configuration, credential, deployment, production system, external account, purchase, pricing, or submission changed; only this handoff status note changed.

## Rev 55 — complete native-to-unlisted delivery roadmap

- Delivery order is now fixed: checkpoint and field-baseline the current web beta; establish Apple identity/naming/legal/privacy plus isolated staging; create a SwiftUI/Supabase native shell; build and calibrate background Long Run/Splits; secure the two-user subscription VM worker with fallbacks; port complete nutrition/barcode/photo behavior; port WHOOP, HealthKit, workouts, timers, and remaining UI; conduct two-user TestFlight and vendor evaluations; then submit the final build and request unlisted App Store distribution.
- Native acceptance gates include crash-safe background/locked-screen location, live/average/split pace, manual/automatic laps, auto-pause, HealthKit workout/routes, field calibration against measured 400 m/1 km/3–5 km courses, session/auth persistence, accessible safe-area UI, and no duplicate or lost activity saves. Preserve the web app as a regression reference until native parity is demonstrated.
- VM gates include private ingress, Supabase JWT plus explicit two-user allowlist, separate provider identities/runtime profiles, bounded job queue/concurrency, temporary-photo deletion, redacted monitoring, authentication-expiry alerts, and an interchangeable subscription/API/self-hosted provider adapter. Nutrition gates include editable/provenance-preserving entries, meal/group/saved-item semantics, native camera scanning, provider fallbacks, label OCR/manual correction, duplicate-safe photo retries, and a weighed-meal/personal-barcode benchmark before quality claims.
- Release prerequisites include Apple Developer Program enrollment, final name/bundle-ID collision review, public support/privacy/terms and account-deletion surfaces, truthful App Privacy declarations for health/location/photos/provider processing, equivalent privacy-preserving login where required alongside Google, in-app account deletion, App Review demo access, TestFlight sign-off, final security/RLS/secret/retention review, and an unlisted-distribution request only after the app is production-ready. Nutritionix/FatSecret/Wahoo approvals remain optional external gates and are activated in Hyper-Dev only after written terms and benchmark review.
- Exact immediate actions: run the existing outdoor web GPS matrix and preserve diagnostics; checkpoint the current validated GPS diff; choose Apple individual versus organization enrollment; obtain final-name/domain/business-email inputs; document both iPhone/iOS versions and VM OS/CPU/RAM/GPU/network; then create the native product specification and Xcode project. No source, schema, dependency, VM, cloud configuration, credential, deployment, production system, external account, purchase, pricing, or submission changed; only this handoff delivery plan changed.

## Rev 54 — unlisted native end-state and private VM inference

- Confirmed target is an App-Review-approved unlisted native iOS app with server-side allowlisting for two accounts. Native Core Location/HealthKit remains capable of all requested recorder behaviors and can target Strava-comparable measured outdoor performance, subject to device/environment limits and a real calibration corpus.
- A continuously running subscription-authenticated photo worker is technically possible on the owner's VM without model API keys because Codex CLI/app-server and Claude Agent SDK/`claude -p` support trusted automation paths. It must be treated as a private adapter rather than a guaranteed permanent product entitlement: OpenAI documents API-key auth as the default for programmatic automation and warns not to expose Codex execution in untrusted/public environments; Anthropic currently permits third-party Agent SDK use against subscription limits after pausing a June 2026 change, but says credits are per-user and recommends API keys for shared production automation.
- Never pool one consumer identity across the two users. A subscription VM design needs a separate OS/runtime credential profile per authorized user, or must be owner-only. It also needs a tailnet/private ingress, Supabase JWT verification plus a two-user server allowlist, job isolation, bounded concurrency, encrypted secrets/disk, temporary-photo deletion, redacted logs, health/restart monitoring, auth-expiry alerts, and no generic shell/tool surface exposed to the app. A CPU-only VM is sufficient for the current remote Codex/Claude CLI calls; self-hosting an open-weight vision model instead would require adequate GPU/RAM and would not automatically match frontier accuracy.
- Preserve a provider interface with the subscription VM as the initial private primary and an optional budget-capped OpenAI/Anthropic API or licensed self-hosted model fallback. This is necessary so photo logging does not disappear if subscription auth, model availability, quotas, or provider terms change. Continue editable review and database/label grounding because even frontier vision cannot infer portion mass/macros perfectly.
- The complete product remains feasible: native GPS/intervals/background execution, WHOOP OAuth import/enrichment and deletion suppression, HealthKit weight/workout access, native camera/barcode flow, manual/saved/grouped nutrition, timers/notifications, and vendor-backed lookup. Nutritionix remains approval/trial gated and must be an optional provider behind FatSecret/USDA/manual/AI fallbacks; an unlisted release does not improve its license. No source, schema, dependency, VM, cloud configuration, credential, deployment, production system, external account, purchase, pricing, or submission changed; only this handoff research/architecture note changed.

## Rev 53 — native GPS, AI, vendor, and two-user distribution boundaries

- A native iOS recorder can implement the requested background/locked-screen route capture, live/average pace, Long Run and Splits, manual/automatic laps, auto-pause, local crash recovery, HealthKit workout/route writes, and later Watch/audio/haptic support. It can target comparable open-sky accuracy to Strava after measured calibration, but cannot guarantee perfect or universally superior GPS because both apps share phone GNSS/environmental limits; short track intervals remain a harder accuracy case.
- A polished native TestFlight build may modestly strengthen Nutritionix's confidence that Hyper is a real qualified commercial evaluation, but native technology itself does not change the two-MAU Business Trial eligibility or make the trial permanent. The stronger application signals remain an honest business identity/email, public landing/privacy/terms pages, working demo, clear server-side/attribution/no-caching compliance, realistic traffic, and an explicit plan to negotiate production terms. Do not use permanent-beta or artificial pricing as a licensing workaround.
- Durable frontier photo logging in a released/unlisted app requires a private server-side OpenAI Responses and/or Anthropic Messages API adapter, with provider secrets outside the binary, authenticated jobs, short-lived photo storage/deletion, rate limits, per-user budgets, structured editable output, and database/label grounding. OpenAI and Anthropic both support image analysis through their APIs, while consumer subscription and API billing are separate. The existing subscription-authenticated Mac worker can continue as an owner-only private-beta bridge while the Mac is online; OpenAI does not document it as a mobile-product backend. Anthropic currently says Agent SDK/`claude -p`/third-party usage may still draw from subscription limits after pausing a June 2026 change, but credits are per user and Anthropic recommends API keys for shared production automation, so do not pool one user's account across testers.
- The correct two-user distribution is internal TestFlight during active development: up to 100 App Store Connect users, with every build expiring after 90 days and replacement builds required. Apple says TestFlight betas should be intended for eventual public distribution, so it is not a perpetual loophole. After the app is complete, an approved unlisted App Store release plus a two-account server allowlist is cleaner; anyone with the direct link can download, but only allowlisted accounts can use it. Setting an extreme App Store price neither caps users nor changes AI/database licenses. No source, schema, dependency, cloud configuration, credential, deployment, production system, external account, pricing, purchase, or submission changed; only this handoff research/architecture note changed.

## Rev 52 — native background-run and automatic-import decision

- Automatic Wahoo export/import is supported only through an approved Wahoo Cloud API application. Its OAuth API exposes workout download plus `workouts_read` and `offline_data`, so Hyper could refresh authorization and sync on sign-in without a per-run file step. Without approval there is no supported fully automatic bridge; the safe fallback is user-initiated FIT export/import, not email scraping, credential sharing, or private API automation.
- The iPhone website remains the wrong final runtime for a dependable locked-screen/background run recorder. Apple documents that native iOS apps can declare the Location Updates background mode, receive Core Location background updates, set an activity type, and build HealthKit workout routes; typical apps are otherwise suspended in the background. Therefore proceed toward a native iOS client if background runs are a core requirement.
- Recommended migration is incremental rather than delaying all product testing for a complete rewrite: preserve Supabase and domain behavior; build the native authentication/session shell and Long Run/Splits recorder first; add crash-safe local trace persistence, background Core Location, HealthKit workout/route writes, live/average pace, lap interpolation, auto-pause, and device field calibration; then port nutrition, WHOOP, and remaining screens. Add FIT-first import as the universal manual fallback and Wahoo Cloud sync only if approved. No source, schema, dependency, cloud configuration, credential, deployment, production system, external account, or form submission changed; only this handoff research/architecture note changed.

## Rev 51 — free external run-import options

- Current official Strava developer documentation changed the prior assumption: a free Strava account can still record runs and export the owner's original/FIT, TCX, or GPX files, but creating a new API application now requires a Strava subscription. New apps begin in single-player mode; the owner can then raise capacity to 10 in the API dashboard. Therefore direct automatic Strava-to-Hyper sync is not a zero-cost new integration, while manual file import can remain free.
- The best no-cost iPhone recorder/import candidate found is the Wahoo app. It is free to install, records outdoor GPS, exposes workout/lap/average pages, supports auto-pause, auto-lap, and a manual Lap button, and can export/share completed activities as FIT files. Hyper does not currently parse FIT/TCX/GPX, so the immediate reliable path requires adding an authenticated, duplicate-safe FIT-first file importer; FIT should preserve route samples, timestamps, and recorded laps better than a GPX-only importer.
- Wahoo also publishes an OAuth Cloud API with `workouts_read` and `offline_data`, explicitly for mobile/web applications and workout downloads. Access is request-and-approval gated, and the public page does not state a subscription fee. If approved, it could support automatic Wahoo-to-Hyper sync on login without Strava; approval and production terms must be verified before relying on it.
- Recommended sequence: add FIT-first manual import and benchmark Wahoo against Hyper on the same measured courses; separately apply for Wahoo Cloud API access; add automatic sync only after approval. A free Strava recording can also be imported manually from an exported file, but Strava exports are currently website-only. No source, schema, dependency, cloud configuration, credential, deployment, production system, external account, or form submission changed; only this handoff research note changed.

## Rev 50 — quality-aware web GPS recorder

- Rebuilt the web run ingestion path around a traceable, quality-aware fusion algorithm. Fresh iPhone browser speed and coordinate displacement are blended when they agree; plausible speed bridges frozen/noisy/spiking coordinates; long gaps never extrapolate from one stale speed; accuracy, stationary, jitter, stale, and teleport rejections remain explicit. The run clock now begins only after three consecutive acceptable fixes, so GPS acquisition time no longer corrupts the first split or average pace.
- Added optional default-on auto-pause using platform speed plus sustained phone-motion evidence. Stops are excluded from active time, average pace, and lap time; resuming re-anchors before accepting distance so stationary drift cannot become a jump. Manual pause/resume, manual splits, auto-split boundary interpolation, legacy snapshots, and idempotent saves remain compatible.
- Added a complete local-only diagnostic trace for every location sample: timestamp, coordinates, accuracy, platform speed, motion evidence, accept/reject decision, accepted increment, and cumulative distance. Active trace recovery snapshots are throttled separately to 30 seconds and capped at 18,000 points to avoid blocking the five-second state recovery path. Coordinates are never mapped into Supabase; they are cleared after save/discard and are exported only through the explicit finished-run diagnostic button. Finished summary recovery omits the large trace but retains its quality summary.
- Live UI now shows current accuracy with acquiring/weak/OK state. Finished runs show typical accuracy, platform-speed coverage, longest callback gap, a Safari-suspension warning above eight seconds, and private JSON diagnostic export. Added a default-on accessible Auto-pause switch. Run segmented controls and Back now meet the 44 px touch-target floor.
- Added deterministic coverage for zig-zag inflation, a large coordinate spike with plausible speed, frozen-coordinate slow movement, stationary drift, speed-missing motion auto-pause, pause/resume, lock-timed elapsed time, trace persistence/isolation, and quality summaries. Final validation PASS: `npm run test` (34 files, 347 tests), `npm run lint`, `npm run build`, and `git diff --check`. Existing non-blocking large-chunk and stale Browserslist warnings remain.
- Responsive browser QA PASS at 390×844: setup and live views have zero horizontal overflow; all interactive targets are at least 44 px; the Auto-pause switch toggles correctly; a stationary-drift replay auto-pauses at 0 m; interrupted-run recovery appears after reload; and browser warning/error logs are empty.
- This is ready for measured outdoor iPhone testing, not a validated claim of Strava parity. Exact test sequence: install/open the latest private build, allow Precise Location and Motion, wait for the timer to leave `0:00`, then record (1) 10 minutes stationary outdoors, (2) a measured 400 m track lap, (3) a known 1 km route, (4) a 3–5 km run with turns/trees, (5) stop/resume with Auto-pause, (6) 400 m automatic and manual splits, and (7) a brief lock/background interval. Run Strava over the same courses in separate matched trials, record distance/split errors, and export Hyper's diagnostic JSON for every miss. Safari can still suspend foreground JavaScript/location delivery, so native iOS remains required for dependable locked-screen/background parity. No database schema, Supabase data, cloud configuration, credential, deployment, production system, or external account changed.

## Rev 49 — Strava-versus-Hyper GPS gap audit

- Confirmed from current source that Hyper's web recorder requests fresh high-accuracy browser geolocation, receives only timestamp/latitude/longitude/horizontal accuracy/platform speed, reduces phone motion to a boolean slow-movement gate, applies fixed accuracy/speed/jitter thresholds plus 60-second pace smoothing, and interpolates auto-split crossings. It does not retain the full route/sample/accuracy stream in the finished activity; it saves only aggregate session distance/time and lap summaries.
- Strava's public documentation confirms its native iOS recorder keeps a GPS track/distance stream, uses the accelerometer for running auto-pause, displays live GPS accuracy, records in the background, and performs server-side moving-time, distance/outlier smoothing, optional distance correction, and elevation correction after upload. Strava also explicitly says it does not road-snap fitness tracks and cannot reconstruct genuinely missing GPS data. Its exact real-time distance filter is proprietary; do not invent claims about an unpublished algorithm.
- Largest accuracy/reliability gaps are therefore: Safari foreground/update-cadence limits; no native Core Location fitness/background configuration or speed/course accuracy fields; no durable raw route and quality trace; no post-run reprocessing/correction path; no motion-based auto-pause/moving-time engine; only a coarse three-state GPS indicator; and no measured outdoor calibration corpus. Hyper's split-boundary interpolation and idempotent save recovery are already solid foundations.
- Strongest web improvements would be pre-start GPS-lock acceptance, raw trace capture, post-run outlier/smoothing/recalculation, explicit sample-quality diagnostics, and outdoor corpus calibration. They still cannot remove Safari background suspension or expose native Core Location quality/configuration. Native iOS is required for `CLActivityTypeFitness`, background activity sessions, precise continuous tracking, speed/course accuracy, stronger motion fusion, and a credible Strava-parity target. Raw route retention would be sensitive location-data work requiring explicit user disclosure, least-retention design, RLS/auth review, and schema approval before implementation. No source, schema, dependency, configuration, deployment, production system, or external account changed; only this handoff investigation note changed.

## Rev 48 — pre-production test gates and two-user beta boundary

- FatSecret is deferred until approval. Remaining web release gates are real-iPhone verification of: open-sky Long Run and Splits accuracy/recovery; real WHOOP connect, repeat sync, enrichment, deletion suppression, and reconnect; real OpenAI/Claude food-photo and researched-description accuracy with editable review and duplicate-safe retry; current barcode decode-versus-catalog-miss behavior; meal ordering, saved-template immutability, safe-area opacity, touch/overflow/accessibility behavior; workout/program/history regressions; and reload/offline/session persistence. Hyper-Dev Google OAuth remains an explicitly accepted untested staging risk rather than a pass. Run `npm run test`, `npm run lint`, and `npm run build` again after fixing any device findings.
- The current website can target strong open-sky performance but cannot reliably equal or beat Strava. W3C browser geolocation makes high accuracy only a hint, may omit speed, and leaves update cadence/provider behavior to Safari; browser suspension also prevents dependable locked-screen/background recording. Native iOS can target Strava parity with Core Location/Core Motion, background execution, activity type, accuracy diagnostics, persistence, filtering, and measured calibration, but the same phone GNSS and environmental limits mean universal superiority cannot be promised and indoor apartment movement is not a valid GPS benchmark.
- A two-person native beta can be maintained with internal TestFlight, but each build expires after 90 days, so this requires continuing Apple Developer membership and replacement builds. Ad Hoc distribution to registered devices is another private option with annual signing/provisioning maintenance. Neither distribution route changes third-party licensing.
- Current OpenAI guidance supports ChatGPT subscription sign-in for local Codex work but recommends API-key authentication for programmatic workflows; therefore the existing trusted private Mac worker can remain a development bridge, not a guaranteed permanent/released-app backend. Current Anthropic guidance explicitly says Agent SDK, `claude -p`, and third-party app use can draw from each user's own subscription limits, but Anthropic says this policy is in transition. Never pool/share credentials or ship CLI/API secrets in the app. Durable architecture is a provider adapter with per-user authenticated private companions where supported and a server-side metered API fallback with budgets.
- Nutritionix's two-MAU offer is an approval-gated Business Trial, not documented perpetual production access. Keeping TestFlight at two users does not make the trial permanent or bypass its intended evaluation terms. Obtain written duration, quota, caching/history, and production-use terms before relying on it; do not design a “forever beta” to evade vendor limits. For now, complete the existing web tests and evaluate FatSecret after approval. No source, dependency, cloud configuration, credential, deployment, production system, or external account changed; only this handoff research note changed.

## Rev 47 — domain availability and brand-risk check

- A live Verisign RDAP check on 2026-07-19 reported `roeslerlabs.com` available. It is the recommended inexpensive business/developer domain because it is a conventional `.com`, supports `alex@roeslerlabs.com`, remains useful if the app name changes, and avoids tying the vendor identity to a crowded product name. Availability is not reserved and can change before checkout.
- Avoid `hyperfitapp.com` despite current registry availability: an active US App Store product named `HyperFit: Lift & Fuel Tracker` already overlaps Hyper's workout/nutrition scope, while other Hyper/HyperFitness products also exist. This is an initial collision screen, not trademark clearance; perform formal naming/trademark review before a public release.
- Current Porkbun official pricing lists non-premium `.com` registration and renewal at US$11.08/year including ICANN fees. Cloudflare Registrar sells at registry/ICANN cost without markup and may be marginally cheaper, but its exact checkout price was not publicly exposed in the checked page. Recommendation for the simplest transparent purchase is Porkbun at the displayed annual price; use Zoho Free or existing iCloud+ for the mailbox. No domain was purchased or reserved and no DNS, mailbox, external account, form submission, source, credential, cloud configuration, dependency, deployment, or production system changed; only this handoff note changed.

## Rev 46 — no-cost custom-email options

- A stable professional custom address cannot normally be completely free unless the owner already owns a domain; domain registration remains the unavoidable cost. After that, Zoho Mail currently advertises a free custom-domain plan for one domain/up to five users with 5 GB each and web-only access, but availability is limited to selected data centers/regions. This is the preferred zero-additional-email-cost option when available.
- iCloud+ Custom Email Domain is also no additional email cost if the owner already subscribes to iCloud+, but still requires an owned domain. Cloudflare Email Routing is free for inbound forwarding only; arbitrary outbound mail requires a paid plan/SMTP provider, so routing alone is not an adequate Nutritionix reply mailbox. No domain, mail service, DNS, mailbox, external account, form submission, source, credential, cloud configuration, dependency, deployment, or production system changed; only this handoff note changed.

## Rev 45 — custom-domain mailbox setup

- `hyper.com` is already owned and operated by an unrelated digital-infrastructure/manufacturing company, so the owner cannot create `alex@hyper.com` without that registrant's authorization. Choose and purchase a distinct available product domain; do not imply affiliation with the existing company.
- For iCloud+ the supported setup is: own/purchase the domain, ensure Apple Account 2FA and primary iCloud Mail are enabled, add it under iCloud Custom Email Domain, apply/verify the requested DNS records, then create a send-and-receive address such as `alex@<owned-domain>`. Use that stable mailbox for Nutritionix and future vendor accounts. No domain availability beyond `hyper.com`, trademark clearance, domain purchase, DNS, mailbox, external account, form submission, source, credential, cloud configuration, dependency, deployment, or production system changed; only this handoff note changed.

## Rev 44 — business-email prerequisite

- Nutritionix rejected the owner's Gmail address, so the trial application requires a real custom-domain mailbox. Do not use a disposable address or fabricate company identity. Fastest legitimate path for this Mac/iPhone owner is an existing owned domain plus iCloud+ Custom Email Domain, which supports sending and receiving through Apple Mail; otherwise purchase an appropriate domain first or use another full custom-domain mail host.
- Recommended address is a personal business mailbox such as `alex@<owned-domain>` rather than a no-reply alias, because Nutritionix will follow up there. A forwarding-only alias is insufficient unless replies can also be sent authentically from the same domain. No domain, mailbox, DNS, external account, form submission, source, credential, cloud configuration, dependency, deployment, or production system changed; only this handoff note changed.

## Rev 43 — Nutritionix trial application guidance

- The current Nutritionix request page confirms the two-MAU Business Trial is approval-gated, not automatic self-service access, and is limited to qualified commercial, research, or enterprise evaluations. Hyper can apply honestly as a pre-launch commercial product/private two-user beta; do not claim incorporation, customers, revenue, or a public website that do not exist.
- Recommended form identity is `hyPer (pre-launch product)` for Company Name, the owner's real phone and preferably professional/business email, and a blank website field if no public URL exists. Do not submit the private Tailscale development URL. The use-case copy should state direct web/iOS nutrition logging, UPC/EAN lookup plus food search/natural-language evaluation, two MAUs, server-side credentials, required attribution, same-user historical logs only, no database replication/resale/cross-user cache, and intent to discuss commercial terms before wider release.
- Trial access and production permission remain unverified until Nutritionix approves and states duration, quota, and commercial-release terms. No form was submitted and no external message, source, credential, cloud configuration, dependency, deployment, or production system changed; only this handoff note changed.

## Rev 42 — three-provider barcode waterfall decision

- Approved technical direction for barcode lookup is a provenance-preserving waterfall: decode once on-device, normalize GTIN variants, query Nutritionix first during the two-user trial, then FatSecret after Premier approval/terms resolution, then USDA Branded. Stop at the first complete exact match; continue on a documented not-found or incomplete serving/macros response. Timeouts, authentication failures, and rate limits may use fallback for availability but must remain observable and must not be mislabeled as catalog misses.
- Never merge macro fields from different providers into a synthetic record. Every review/log row retains provider, provider item/serving identity, lookup barcode, serving basis, attribution, and the exact logged snapshot when the provider permits it. Nutritionix permits same-user historical food-log retention but not a shared cache; FatSecret publicly permits most non-ID values for only 24 hours, so long-term FatSecret history/saved-food behavior remains gated on written approval or a provider-profile/diary design; USDA is CC0/public domain.
- Current rollout sequence: Nutritionix → USDA in Hyper-Dev as soon as the trial credentials are configured server-side; insert FatSecret between them only after Premier access, static-egress/credential handling, attribution, and historical-storage terms are resolved. Benchmark per-provider hit rate and accuracy on the same 50–100 real products before making the permanent priority order. No source, dependency, credential, cloud configuration, deployment, or production system changed; only this handoff research/decision note changed.

## Rev 41 — Nutritionix two-user trial assessment

- The current Nutritionix Business Trial shown by the owner is a viable direct provider for Hyper's private two-user beta: it advertises up to two MAU plus natural-language and barcode endpoints. This would make Hyper query Nutritionix directly; it does not create an import path from Cronometer or Nutritionix Track.
- Each real user must be represented honestly through `x-remote-user-id`; do not collapse multiple users into the development `0` identity to bypass MAU accounting. Keep the Nutritionix app key server-side and add visible Nutritionix attribution to every interface that uses its results.
- The pricing table says general caching is unavailable, but Nutritionix's official FAQ explicitly pre-approves retaining nutrients for a historical food-log transaction for that same user. Conservative implementation: persist logged-entry snapshots for history; do not cache search results or build a shared catalog; store provider identity and re-fetch reusable saved-food/template data unless Nutritionix gives broader written permission. Manual/user-authored data remains separate.
- The public screenshot does not establish trial duration, production rights, or request quota. Treat this as staging/private-beta access until the account terms or Nutritionix confirm those points. Keep FatSecret Premier Free in progress as the likely scalable path and benchmark both providers on the owner's real barcode corpus. No source, dependency, credential, cloud configuration, deployment, or production system changed; only this handoff research note changed.

## Rev 40 — external diary auto-import research

- Official documentation still exposes no supported consumer-diary OAuth/API route from Nutritionix Track or Cronometer to Hyper. Nutritionix's developer endpoints provide food search, UPC lookup, and nutrient parsing, while Track offers a user-initiated emailed export. Cronometer documents CSV export and names only Everfit, Kalix, and Practice Better as API partners. Do not build credential sharing, scraping, or browser automation around either service.
- FatSecret's own consumer app is the one documented web-compatible bridge found. Its full 3-legged OAuth lets an existing FatSecret app/site member authorize Hyper, and `food_entries.get.v2` returns dated item-level diary entries with stable entry/food/serving IDs, meal, serving quantity, calories, and macros. Subject to FatSecret approval and terms, Hyper could idempotently pull the current/recent diary on login, foreground, manual sync, and a scheduled reconciliation. No documented diary webhook was found, so this is pull-based rather than guaranteed instant push.
- A native iOS Hyper app changes the Cronometer fallback only partially: Cronometer documents exporting 13 nutrients to Apple Health, and native HealthKit can read authorized nutrient samples. This may support automatic totals/timestamps, but Cronometer does not document preservation of original barcode, product/serving identity, or meal bucket in HealthKit. The current website cannot read HealthKit directly.
- Recommendation remains to make Hyper the primary logger with direct FatSecret barcode lookup after approval. Optionally add FatSecret-member diary import as a supported interim/migration feature. Treat native Cronometer-to-HealthKit import as an aggregate fallback, not a lossless item diary. No source, dependency, credential, cloud configuration, deployment, or production system changed; only this handoff research note changed.

## Rev 39 — barcode stack recommendation

- The recommended no-database-fee native stack remains Apple VisionKit/DataScanner for optical decoding plus FatSecret Premier Free for US packaged-food lookup, USDA Foundation/FNDDS for generic ingredients, and nutrition-label OCR/manual correction for misses. This is the strongest practical free fit found, not a guaranteed universal winner; adoption remains gated on the 50–100 item personal benchmark.
- For maximum performance regardless of price, separate the problem: Scandit is the leading enterprise decoder candidate based on its current published damaged/curved/low-light/OCR fallback capabilities and vendor-reported greater-than-99% targeted-code accuracy. The maximum-coverage data layer should negotiate NIQ Label Insight or comparable enterprise catalog access, retain FatSecret Premier for consumer-ready foods/servings/restaurants, use GS1 only to verify product identity, and keep provider-specific results legally separated. No official independent benchmark proves one universal database winner.
- Best value decision: apply for FatSecret Premier Free now, build native VisionKit first, measure the real corpus, and pay for Scandit or enterprise product data only if the measured decode or catalog miss rate justifies it. No source, dependency, cloud configuration, credential, deployment, or production system changed during this research; only `handoff.md` changed.

## Rev 38 — FatSecret-ready pre-iOS checkpoint

- Audited the current uncommitted iPhone beta against the requested nutrition product. Existing implementation already covers editable manual logging, cited OpenAI/Claude researched descriptions, color one/two-angle photo analysis with portion context and editable item review, saved-food creation/edit/deletion with historical immutability, default/chronological meal buckets, provenance, USDA generic search, and layered staging barcode lookup.
- Added a pure, provider-neutral FatSecret `food.find_id_for_barcode.v2` response mapper. It handles array/singleton servings, requires all four primary macro values, prefers the provider-flagged/default package serving over 100 g, preserves mass/volume units and labels, includes brand attribution in the name, and stores only the explicitly storable `food_id:serving_id` identity. Added three regression tests and forward-compatible `fatsecret`/`barcode_fatsecret` provenance types/labeling.
- Added `docs/fatsecret-premier-free-application.md` with the exact current form sequence, copy-ready inquiry, six written clarification questions, attribution/storage requirements, architecture boundary, 50–100 product benchmark, quantitative go/no-go gates, and activation checklist.
- Confirmed an infrastructure constraint before wiring credentials: FatSecret requires client-credential token requests through an allow-listed proxy, while hosted Supabase Edge Functions do not provide stable egress IPs. The approved implementation needs a small static-egress proxy or a written FatSecret exception. No direct phone credential, unreliable Edge call, provider key, or production provider activation was added.
- Full validation PASS after the additions: `npm run test` (34 files, 342 tests), `npm run lint`, `npm run build`, targeted FatSecret test/lint, and `git diff --check`. Build retains only the existing non-blocking large-chunk and stale Browserslist warnings.
- No dependency, schema, cloud secret, remote configuration, deployment, production system, or running private worker changed. Exact next external action is for the owner to register a Basic FatSecret application, publish the minimal public/legal pages, and submit the Premier Free form using the prepared inquiry. Activate the provider only after written storage/attribution answers and the barcode benchmark pass.

## Rev 37 — native iOS/FatSecret product roadmap

- Official-source research supports a full SwiftUI client, not a web wrapper, if Hyper's targets are native camera scanning, background workouts/rest timers, HealthKit, and Strava-class run recording. Reuse the existing Supabase backend and domain behavior, then port one vertical slice at a time while the web app remains the regression reference.
- FatSecret Premier Free is the preferred production packaged-food candidate if Hyper meets the published startup/student/nonprofit eligibility and the application is approved. It currently advertises the US Premier dataset, unlimited calls, barcode/autocomplete/caching features, required attribution, and vendor-reported 90%+ global UPC/EAN coverage. This does not prove it beats Cronometer, which combines curated CRDB with Nutritionix and USDA. Gate adoption on a 50–100 item corpus of the user's real groceries and obtain written clarification on historical nutrition snapshots/saved-food storage because most returned content is not storable beyond 24 hours under the public guide.
- Native barcode quality has two separate gates: VisionKit/DataScanner optical decode and FatSecret catalog lookup. Proposed targets under good light are at least 98% optical decode and 90–95% catalog matches on the personal corpus; label-photo/manual fallback must remain for misses. FatSecret OAuth credentials and tokens must stay behind the backend proxy.
- Native GPS ownership remains Hyper's Core Location/Core Motion recorder. Target comparable open-sky accuracy through GPS warmup/quality UI, background precise location, raw-sample diagnostics, validity/staleness/accuracy filters, motion-aware stationary logic, robust pace smoothing, exact split-boundary interpolation, crash recovery, HealthKit workout/route export, and months of measured field calibration. Do not promise universal Strava parity or treat indoor apartment movement as a GNSS acceptance test.
- Proposed run quality gates before release: stationary drift under 10–20 m over 10 minutes; 400 m track median lap error at most 2% and 95th percentile at most 3%; known 1 km/5 km open routes within about 2%; no background/locked-screen gaps over 10 seconds in a 60-minute run; no duplicate or lost session after interruption/retry. Intervals add distance/time/repeat/rest presets, manual lap override, boundary interpolation, audio/haptic cues, and Live Activity/rest countdown.
- Production WHOOP sync should retain server-side tokens, request `offline`, serialize rotating-token refreshes, accept signed/idempotent v2 webhooks, upsert provider records, and run app-open plus scheduled reconciliation because WHOOP documents duplicate/missed webhooks. Link overlapping Hyper GPS runs to WHOOP summaries instead of duplicating them; retain Hyper notes and durable suppression for ignored walks. WHOOP supplies completed strain/HR/distance/zones, not route/laps/live HR. Optional BLE heart-rate broadcast can enrich native Hyper runs.
- Add HealthKit after the core client: anchored/observer imports for EufyLife-authored weight/body composition, source/UUID dedupe, and an optional Cronometer nutrient-total bridge. Exact Cronometer barcode items and meal buckets remain unverified, so Hyper should become the primary nutrition logger once FatSecret passes its benchmark. Apple Watch is a later phase for wrist cues/live HR and reliable swim laps/distance; WHOOP alone cannot provide those.
- The private subscription-authenticated Codex/Claude Mac worker remains acceptable for personal development only. OpenAI and Anthropic both state that consumer subscriptions and API billing are separate. A released iOS app needs server-side API credentials/budgets or on-device inference; no AI provider secret may ship in the app. Preserve color, two-angle editable photo review and researched-food citations, and ground estimates against FatSecret/USDA/package labels.
- Release sequence: freeze and checkpoint the validated web beta; prepare public landing/privacy/terms/support and FatSecret attribution; apply for FatSecret while enrolling in the Apple Developer Program; create isolated dev/staging/prod native configurations; build native auth/offline shell; port Today/Train/Fuel/You; add VisionKit + FatSecret and benchmark; implement GPS/interval engine and field corpus; add WHOOP webhooks/dedupe; add HealthKit and AI backend; harden accessibility/privacy/security/battery/recovery; dogfood through TestFlight; then submit to the App Store.
- No source, dependency, schema, cloud configuration, production system, or running worker changed during this research. Only `handoff.md` changed. Exact next action: finish the current real-iPhone web QA and make a safety commit, then produce the one-page native product spec plus FatSecret application package before starting Xcode work.

## Rev 36 — pinned high-effort food-worker models

- The private food worker now explicitly uses OpenAI `gpt-5.6-sol` with `model_reasoning_effort="high"` and Anthropic `claude-opus-4-8` with `--effort high` for both photo analysis and researched food descriptions. Environment overrides remain available through the existing/new `PHOTO_WORKER_*` variables.
- The installed PATH Codex CLI remains `0.142.0` and does not support GPT-5.6-sol. No global package was upgraded. On macOS the worker now prefers the authenticated Codex binary bundled with the ChatGPT desktop app (`0.145.0-alpha.18` in this session), with `PHOTO_WORKER_CODEX_COMMAND` or PATH `codex` as fallbacks. This removes the local version blocker without changing user-level dependencies.
- Worker `/health` now returns separate `models` and `efforts` maps, and Settings → Food analysis displays both so the iPhone can verify the exact configuration. Added client regression coverage for this response.
- Direct provider probes PASS: bundled Codex accepted `gpt-5.6-sol` + high and returned `MODEL_OK`; Claude Code `2.1.211` accepted `claude-opus-4-8` + high and its result metadata confirmed Opus 4.8 usage. Full live `/describe` probes also PASS for both providers using the same large hard-boiled egg input; each returned a schema-valid 78 kcal estimate with real sources and reported the requested model.
- Restarted the real Hyper-Dev worker on `127.0.0.1:8788` (PID 4471 at launch) with the Hyper-Dev public client configuration loaded only in process memory. Local and Tailscale port-8443 health checks both PASS, both providers are authenticated, and both report the exact model/high-effort settings. Production was not targeted or changed.
- Files changed in this revision: `scripts/photo-food-worker.mjs`, `src/lib/photoAnalysis.ts`, `src/pages/Settings.tsx`, `tests/photoAnalysis.test.ts`, and `handoff.md`. Validation: `npm run test` PASS (33 files, 339 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. Existing non-blocking large-chunk and stale Browserslist warnings remain.

## Rev 35 — FatSecret Premier Free approval assessment

- Current FatSecret documentation makes Hyper a strong product-use fit but approval is not automatic. Premier Free is manually verified and limited to startups under US$1M annual revenue and under US$1M raised, nonprofits, or students/student research groups. It provides the US dataset, Premier barcode/autocomplete features, and required attribution; FatSecret's current editions table advertises unlimited Premier Free calls, while its general terms still mention a 5,000-call daily limit, so the actual approved quota must be confirmed in writing.
- Candid planning estimate, not a published FatSecret acceptance rate: roughly 30–50% if submitted today as a private personal beta with no public product/legal pages or clearly eligible organization, and roughly 65–80% after presenting Hyper as an eligible pre-launch startup with a public landing page, privacy policy, terms, working demo/screenshots, realistic traffic, and an explicit attribution/compliance plan. If Hyper is neither a startup/company nor a student/nonprofit project, Premier Free eligibility is uncertain and should be confirmed before relying on it.
- “Getting the database” means revocable API access, not a downloadable or freely copyable database. Barcode lookup is Premier-only. FatSecret claims over 90% global UPC/EAN coverage, but this is the vendor's aggregate claim rather than a Hyper-specific measured US hit rate.
- The most important architecture issue is persistence: FatSecret's public terms require most returned content to be removed or refreshed within 24 hours, and its storable-data guide lists identifiers such as `food_id` and `serving_id`, not food names or macro values, as indefinitely storable. Before implementation, request written confirmation for Hyper's historical diary/saved-food behavior or design around provider IDs plus re-fetch/FatSecret diary endpoints. Do not bulk copy, train models on, resell, or silently merge the catalog.
- Premier Free attribution must appear wherever FatSecret content is displayed, on a public page even when the app requires login, on the app website, and later in the App Store listing. Hyper currently has no in-repo public landing page, privacy/terms pages, FatSecret attribution, credentials, or client, so it is not yet application-ready even though the core food logging flow is a credible demo.
- Best next application sequence: self-register for Basic and obtain client details; publish minimal landing/privacy/terms pages; prepare screenshots or a short demo; apply for US Premier Free as `Hyper` with exact barcode/search use, low initial traffic, revenue/funding eligibility, and no bulk-replication/model-training language; explicitly accept attribution; ask about nutrition snapshot storage and the quota discrepancy in the same inquiry. No app source, dependency, credential, remote configuration, staging, or production system changed during this research.

## Rev 34 — verified native iOS integration outcomes

- Corrected the prior Cronometer conclusion after rechecking current first-party documentation. Cronometer states that its iOS Apple Health integration exports 13 nutrients. A native Hyper app can observe and incrementally read Cronometer-authored HealthKit nutrition samples, including deletions, with HealthKit anchored/observer queries and optional background delivery. This cannot be done by the current web app.
- Automatic Cronometer numerical nutrition import is therefore feasible after native conversion, subject to a one-device probe confirming exactly which Cronometer write permissions/sample types are present. HealthKit supports dietary energy, protein, carbohydrate, fat, food correlations, and optional food-type metadata, but Cronometer's public documentation does not establish that it writes original barcode, serving, diary group, or complete food identity. Expect automatic totals/timestamped nutrient entries; preserving exact Cronometer item names and meal groups is unverified and may remain impossible without Cronometer API/CSV access.
- Current EufyLife documentation explicitly says each weighing can write Weight, Body Fat Percentage, BMI, Height, and Lean Body Mass to Apple Health after opt-in. Eufy support notes that the scale must be paired to the main user's EufyLife account, Apple Health connected under third-party services, and only the main EufyLife user syncs. Native Hyper can then automatically import at least HealthKit body-mass samples; delivery still depends on EufyLife first writing them.
- Native iPhone GPS can use the same iPhone GPS hardware/Core Location source available to Strava, plus Core Motion/accelerometer data, background location, precise accuracy diagnostics, GPS warmup, smoothing, and active-time logic. Strava itself documents that its iPhone recorder cannot repair missing GPS, does not road-snap like navigation apps, and uses the accelerometer for running auto-pause. Hyper can target comparable outdoor accuracy, but parity requires a measured field corpus and cannot be guaranteed under tree/building obstruction, multipath, poor phone placement, or indoor conditions.
- Persistent sign-in is feasible and mostly already designed: Supabase sessions can silently refresh and WHOOP's requested `offline` scope provides rotating refresh tokens. Hyper already stores WHOOP tokens only server-side and persists each rotated pair before sync. A native app should keep only its own Supabase refresh credential in Keychain. Reauthentication remains necessary after explicit logout/revocation, invalid/expired refresh state, or provider security intervention.
- Native rest timers are a strong solve: store an absolute deadline, schedule a `UNTimeIntervalNotificationTrigger`, and show a Live Activity countdown on the Lock Screen/Dynamic Island. iOS delivers the local notification even when Hyper is backgrounded or not running; Focus, notification permission, silent settings, or user-disabled Live Activities can still suppress presentation. The current web timer already stores an absolute deadline and restores the correct value, but cannot reliably sound/vibrate while Safari is suspended.
- Native conversion outcome matrix: fully/mostly solves Safari chrome/safe areas, HealthKit Eufy import, secure app-session storage, and background rest alerts; materially improves GPS continuity/diagnostics, camera/scanning, and background workouts; does not itself solve food-database coverage/licensing, photo portion truth, private Codex/Claude worker availability, WHOOP API/live-GPS/lap limits, staging OAuth configuration, or webhook/backend synchronization.
- No source, dependency, schema, auth, staging, production, or remote configuration changed during this investigation. No code validation was required; only `handoff.md` changed and `git diff --check` remains the relevant check.

## Rev 33 — two run modes + native GPS/WHOOP boundary

- Removed Sprints as a startable/user-facing phone tracker mode. `/train/run` now offers exactly `Long run` and `Splits`; the sprint GPS simulator is no longer listed. Legacy sprint engine/state and the `sprint_session` activity type remain readable so old saved or interrupted data is not corrupted.
- Future multi-record fast running clusters imported from WHOOP now classify as `interval_run`, the same as other split sessions. Updated the WHOOP preview fixture and grouping regression test accordingly. Existing historical `sprint_session` rows remain valid.
- Official API research establishes the ownership boundary for the native app: Hyper must use native Core Location plus Core Motion for live distance, speed/pace, average pace, and exact lap boundaries. WHOOP has no built-in GPS; its public API exposes completed workout summaries including duration, strain, average/max HR, energy, total distance/elevation, and zones, but no route, live GPS stream, or lap series. WHOOP can optionally broadcast live HR over BLE, while the API remains post-workout only.
- A production-grade sync should add signed WHOOP v2 webhooks for `workout.updated`/`workout.deleted`, idempotently fetch the changed record, and retain login/app-foreground reconciliation because WHOOP documents duplicate and potentially missed webhook delivery. Current Hyper sync is manual. Current History already supports notes on any activity and soft-dismisses deleted WHOOP imports so re-sync does not resurrect irrelevant walks.
- WHOOP can provide swimming duration/HR/strain but cannot independently measure swim distance or laps. For a known continuous-swim distance, Hyper can combine manual distance with WHOOP duration to calculate average pace per 100 m/yd. Reliable automatic pool laps/open-water distance require a swim-capable watch source such as Apple Watch/HealthKit; WHOOP alone is insufficient.
- Files changed in this revision: `src/pages/RunTracker.tsx`, `src/lib/gpsScenarios.ts`, `src/lib/whoopImport.ts`, `src/preview/whoopFixtures.ts`, `src/types/index.ts`, and `tests/whoopImport.test.ts`.
- Validation: targeted WHOOP/run tests PASS (2 files, 51 tests); full `npm run test` PASS (33 files, 338 tests); `npm run lint` PASS; `npm run build` PASS; `git diff --check` PASS. Existing non-blocking large-bundle and stale Browserslist warnings remain. No dependency, schema, auth, staging, production, or remote configuration was changed.

## Rev 32 — verified food-worker models + researched-food flow

- Live worker/model probes disprove the assumed exact pair. The worker reports OpenAI `Codex CLI default` and Anthropic `fable`. A direct unpinned Codex run identifies the actual model as `gpt-5.5`; explicitly requesting `gpt-5.6-sol` fails HTTP 400 because installed `codex-cli 0.142.0` is too old and must be upgraded. The current npm release is `0.144.6`; upgrading the global package requires explicit approval.
- Direct Claude Code `2.1.211` probes show `--model fable` resolves to `claude-fable-5`, while the documented `--model opus` alias resolves to `claude-opus-4-8`. Fable 5 is not Opus 4.8 and its relative quality is not established by current public Anthropic documentation. Do not silently switch models; choose either the current Fable 5 or the publicly identifiable Opus 4.8 and validate against the same food-research cases.
- The desired searchable-food flow is already implemented: Log Food → Manual → Describe with AI calls `/describe`; Codex uses web search and Claude uses WebSearch/WebFetch; the prompt prioritizes official manufacturer/restaurant pages, then government data, then reputable databases; output requires editable calories/macros, confidence/assumptions, and 1–3 real source URLs. The client fills the Manual form, preselects `Save as reusable meal`, and logging creates/reuses a saved-food record plus the dated nutrition entry.
- Accuracy still depends on an exact product/restaurant, serving amount, preparation, sauces/oils, and source quality. A package or official label must override a model estimate. Model choice alone cannot guarantee correct nutrition values; source grounding, structured validation, user review, and same-case provider evals are the relevant controls.
- No app source, worker configuration, dependency, process, staging, or production change was made during this verification. Pending user approval/decision: upgrade global Codex CLI and pin `gpt-5.6-sol`; choose Claude Fable 5 or explicit `opus` (currently Opus 4.8), then restart and run identical branded/generic/restaurant evaluation prompts through both providers.

## Rev 31 — native iOS transition impact assessment

- Product distinction: merely wrapping the current website removes Safari chrome but does not materially upgrade device data. The intended conversion should preserve the React/domain layer while adding native modules for Core Location/Core Motion, HealthKit, VisionKit camera scanning, secure OAuth callbacks/keychain storage, background execution, and iOS safe-area presentation.
- Native modules would fully solve Safari toolbar/home-indicator underlay behavior and enable direct HealthKit weight import when EufyLife actually writes body-mass samples. They would materially improve GPS continuity/background tracking by combining Core Location diagnostics with CMPedometer distance/pace/cadence, and materially improve barcode decoding through VisionKit.
- Native conversion does not create food-database coverage, Cronometer API access, WHOOP server reliability, Google/Supabase staging configuration, or a production-safe substitute for the private Codex/Claude Mac worker. Photo nutrition/portion accuracy and indoor absolute positioning also remain estimation problems; native sensors improve them but cannot guarantee truth.
- Rough planning estimate across the recurring device/integration issues so far: approximately one-third disappear, one-third materially improve, and one-third remain backend/data/model concerns. GPS specifically should improve substantially, but open-sky validation remains necessary and indoor GNSS can still be poor.
- No source, runtime, dependency, staging, or production change was made for this assessment beyond recording the decision boundary in the handoff.

## Rev 30 — sensor-assisted slow GPS + live average pace

- Root cause of the real apartment test was the rev-13 hard stationary gate: every browser-reported speed below 1.2 m/s was discarded, so legitimate slow movement could stop permanently near 6 m. The 20 m rolling-pace minimum also left the live screen with no pace for a short walk.
- Current research boundary: W3C Geolocation only exposes an accuracy radius and current speed, treats high accuracy as a browser hint, and leaves watch update cadence/"significant change" to the user agent. It does not expose native Core Location speed accuracy or raw GNSS. Current pedestrian-positioning research supports GNSS plus inertial/PDR fusion for blocked environments; full native-grade PDR still needs step-length/heading calibration and lower-level sensors that this iPhone web app cannot reliably access.
- Implemented the strongest low-risk web subset: active runs optionally request iPhone Device Motion permission, classify only sustained acceleration, and use that independent evidence to override the low-speed stationary gate. When confirmed motion and a 0.25–1.2 m/s browser speed coexist, distance integrates the speed for at most five seconds per location update, so a temporarily frozen indoor coordinate can still progress without reintroducing ordinary stationary drift. Motion samples are memory-only, reduce to one boolean at GPS ingestion, and are never stored or transmitted. Denial/unavailability falls back to the existing GPS filter.
- High-accuracy geolocation now requires a fresh fix (`maximumAge: 0`). Free Run falls back from 20 m rolling pace to whole-session average after 5 m, always shows a separate Avg pace metric, and renders very slow early values as `60:00+ /mi` instead of blank. Finished-run/split presentation uses the same run-specific cap.
- New deterministic regressions reproduce a coordinate freezing after approximately 6 m while motion/speed continue, verify approximately 43 m is recorded over the 60-second fixture, preserve zero-distance iPhone-like fidget and stationary drift behavior, verify early average pace, and cover sustained/quiet/expired/gravity-inclusive accelerometer signals.
- Responsive browser QA PASS at 390×844: live Free Run renders Pace plus Time/Distance/Avg pace with `body.scrollWidth === viewport width` (390), no clipping, and no warn/error console entries.
- Validation: `npm run test` PASS (33 files, 338 tests); `npm run lint` PASS; `npm run build` PASS; `git diff --check` PASS. Existing non-blocking large-bundle and stale Browserslist warnings remain. No dependency, schema, staging, production, or remote configuration was changed.
- Exact real-iPhone retest: refresh the private app, start Free Run, allow Motion & Orientation and Location if prompted; (1) remain still for 60 seconds and expect under 5 m, (2) walk continuously for 60 seconds and confirm distance does not stall at 6 m and Avg pace appears after 5 m, then (3) walk/run a measured outdoor 200–400 m route with the screen visible. Indoor satellite reception is not a valid absolute-distance benchmark; the measured open-sky route is the acceptance test. If it still stalls, record whether Motion & Orientation permission appeared/was allowed and the displayed distance/time.

## Rev 29 — private worker health diagnosis

- After the iPhone reported OpenAI Codex's worker as offline, direct checks confirmed the worker is still listening on `127.0.0.1:8788`; both the local and private Tailscale `/health` endpoints return HTTP 200 with OpenAI and Anthropic installed/authenticated (`Codex CLI default`, Claude `fable`).
- The Tailscale port-8443 route still proxies to port 8788, and a health request carrying the app's phone origin receives the correct `Access-Control-Allow-Origin` response. The worker process did not need restarting and the provider login is not the failure.
- Browser E2E from the actual hosted app origin also PASS: `/preview` → You → Food analysis, with the port-8443 URL, returned `Worker connected. Signed in: openai, anthropic. Models: OpenAI Codex CLI default, Claude fable.` This rules out the current client bundle, browser CORS/TLS, worker route, and provider selection as causes.
- Most likely cause is the iPhone browser's per-origin Worker URL reverting to the desktop-only default `http://127.0.0.1:8788` or being opened under a different app origin with separate local storage. On the phone, Settings → Food analysis must use `https://alexanders-macbook-air.taileaf222.ts.net:8443`, then Save and test worker. If that exact value still fails after refresh, capture the complete text following `worker is offline:` for the next diagnosis.
- No source, dependency, configuration, staging, or production change was made during this diagnosis.

## Rev 28 — compact saved-food management + consumed-energy hero

- Fuel's calorie hero now reports `Energy consumed` as the primary figure, with the daily target shown as the comparison. This matches the protein/carbs/fat presentation instead of mixing a remaining-calorie number with consumed macros.
- Log Food → Saved is now a clean browse list by default: every full-width row logs the saved food, while top-level Add and Edit controls handle management separately. Add opens a blank Manual form with `Save as reusable meal` already selected. Edit toggles an explicit manager mode with 44 px pencil/trash targets; Done returns to the clean logging list.
- Saved rows now wrap long names/macro summaries, use strict `min-width: 0` containment, and clip horizontal overflow. The You → Saved meals manager uses the same pencil/trash icon pair, 44 px targets, wrapping behavior, delete confirmation, and historical-log preservation copy.
- Responsive preview QA PASS at 390×844 and 375×812: Fuel displayed 1,342 kcal consumed against a 2,600 target; Saved browse/Edit states and the You manager had zero overflowing elements; Add opened blank Manual with reusable save selected; all management targets measured at least 44 px; browser console errors were zero.
- Validation: `npm run test` PASS (32 files, 331 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. No dependency, schema, remote staging, or production change was made.

## Rev 27 — researched food estimates + layered barcode lookup

- Official-source research confirmed Cronometer's quality is not one superior free database: it combines licensed NCCDB for curated generic foods, USDA/SR data, Nutritionix for barcode products, and its human-reviewed CRDB. NCCDB and Nutritionix are not equivalent free drop-ins. FatSecret Premier Free remains the strongest production candidate if approved; it advertises verified US data and Premier barcode access with required attribution.
- USDA text search now excludes noisy Branded results and uses Foundation, SR Legacy, and Survey/FNDDS for generic foods. Nutrient parsing now selects kcal energy by nutrient ID/unit instead of accepting the first `Energy` row, which could be kJ and materially overstate calories.
- Added Manual → Describe with AI. The user enters food, portion, preparation, brand/restaurant, and ingredients; the private Mac worker uses live web research through either subscription-authenticated Codex or Claude, returns one structured estimate with 1–3 citations/confidence/assumptions, fills editable macro fields, and preselects Save as reusable meal. Nothing is persisted until the user reviews and logs it.
- Real worker E2E PASS for both providers using the same medium hard-boiled egg prompt: both returned 68 kcal / 5.5 g protein / 0.5 g carbs / 4.7 g fat with citations. The installed Codex CLI rejected pinned `gpt-5.6-sol` and `gpt-5.6` under ChatGPT auth, so the worker now lets Codex choose its compatible account/CLI default instead of advertising an unavailable model. No CLI/dependency upgrade was performed.
- Barcode lookup now tries USDA exact GTIN first, then Open Food Facts. Open Food Facts values preserve g/ml serving units, display source attribution during review, and persist provider provenance on the log. Scanner errors distinguish successful barcode decoding from a missing record in both databases.
- Open Food Facts is staging evaluation only: its official ODbL terms require attribution and share-alike when combining databases. Do not carry this fallback to production until the catalog/storage architecture and license compliance are explicitly decided. FatSecret Premier Free is the preferred clean production path after approval/terms review.
- Hyper-Dev only: deployed `food-lookup` ACTIVE version 3 with explicit project ref `nwvgkxqjqihqnjuworqz`; unauthenticated lookup remains HTTP 401. Production ref `nnwfaaxmyvqsdnfcdxom` was not targeted or changed.
- Responsive preview QA PASS at 390×844: AI research expands cleanly, fills/editable macros and citations, auto-selects reusable save, barcode fallback review shows Open Food Facts attribution, document stays fixed with internal scrolling, horizontal overflow is zero, and browser console errors are zero.
- Validation: `npm run test` PASS (32 files, 331 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. Live local app and restarted worker remain available through the existing Tailscale routes.
- Next: on the signed-in iPhone, test the same 7 products again and record whether each failure says the barcode was read but no record was found or never reaches lookup. Send the six failed barcode digits/messages for direct USDA/Open Food Facts coverage comparison. Test Describe with AI once under OpenAI and once under Claude, edit one macro before logging, confirm the item appears in Saved, and confirm a later saved-food edit does not mutate the original log.

## Rev 26 — direct Saved-tab template deletion

- Added a separate delete action beside the direct edit pencil in Log Food → Saved. Each row now has distinct Log, Edit, and Delete buttons; edit/delete are each 44 px wide and the row remains readable without horizontal overflow at 390 px.
- Deletion intentionally removes the item from reusable Saved meals by changing its source to `manual_entry` instead of deleting the `foods` row. This preserves referential integrity and guarantees historical nutrition logs keep their original food name/macros.
- The confirmation explicitly states that past logged entries remain unchanged. While deleting, edit/delete are disabled and the delete icon becomes a spinner. Success and errors now render inside the Saved tab with appropriate live status/alert semantics.
- Preview browser QA PASS: deleted `Salmon Fillet`, confirmed it disappeared from Saved, received `Saved meal deleted. Past logs were not changed.`, retained the three independent action targets, and observed no error-level console entries.
- Validation: `npm run test` PASS (30 files, 325 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. No schema, dependency, remote staging, or production change was required.

## Rev 25 — direct Saved-tab template editing

- Confirmed and preserved the correct two-layer behavior: editing a logged nutrition entry updates only that log's food reference and does not mutate the reusable saved meal; editing a saved template creates a replacement saved-meal record and retires the old record from the Saved list, so existing historical logs retain their original nutrition values.
- Settings already had a complete Saved meals manager for name, calories, protein, carbs, fat, and removal. Added the same edit entry point directly to each row in Log Food → Saved: tapping the main row still opens the normal logging review, while a separate pencil opens the reusable template in Manual with all five fields and the explicit `Update saved meal` action.
- The Saved row now has two valid, non-nested button targets with explicit accessible names. The pencil target is 44 px wide and the full row is approximately 74 px tall on iPhone.
- Preview browser QA PASS at 390×844 and 430×932: edited `Salmon Fillet` into `Salmon Rice Bowl` with 510 kcal / P42 / C55 / F14, received the future-logs confirmation, and returned to Saved where only the replacement values appeared. Logging and editing remained distinct, horizontal overflow was absent, the document stayed fixed under the internal app scroller, and no error-level console entries were observed.
- Validation: `npm run test` PASS (30 files, 325 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. No schema, dependency, remote staging, or production change was required.

## Rev 24 — default named meals + chronological numbering

- Breakfast, Lunch, and Dinner are now lazily created and persisted whenever a day without all three defaults is opened. This uses the existing `nutrition_groups` table and unique named-label index; no schema, migration, function, or production change was required.
- Generic meals are labeled by their chronological position among every meal section, including named meals. The requested sequence renders exactly `Meal 1`, `Meal 2`, `Breakfast`, `Lunch`, `Meal 5`, `Dinner`, `Meal 7`. Snacks remain independently numbered and do not consume meal positions.
- Adding a generic meal or snack now inserts it by the current local time against stable named-meal anchors: Breakfast 08:00, Lunch 12:00, Dinner 18:00. Repeated additions in the same time window append within that window. Existing earlier/later controls remain available and renumber generic meals immediately.
- Named default meals no longer show delete controls and the Add sheet now contains only Meal and Snack. Custom meals/snacks remain deletable. The preview fixture now includes Dinner so today's fixture starts with all three defaults.
- Added pure regression coverage for missing defaults, all four time windows, renumbering across named meals, and the exact seven-position example. Browser QA PASS at 390×844 and 430×932: an empty date materialized Breakfast/Lunch/Dinner, two early additions became Meal 1/Meal 2 before Breakfast, default delete actions were absent, custom delete actions remained, the app internal scroller stayed intact, horizontal overflow was absent, and no error-level console entries were observed.
- Validation: `npm run test` PASS (30 files, 325 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. Tailscale app and worker remain HTTP 200, and the live served client contains the new behavior. Production was not targeted or changed.

## Rev 23 — iOS 26 Safari clipped-scroll workaround

- The rev-22 fixed-nav underlay failed on the user's real iPhone: Safari's floating bottom controls still revealed `Set manually` and its fields below the app navigation. This is consistent with a documented iOS 26 Safari behavior that clips fixed/sticky descendants when they extend below the browser controls; increasing or restacking an out-of-viewport strip is not a reliable fix.
- Replaced the document-scrolling layout with a scoped fixed, opaque app shell and a full-height internal `<main>` scroll viewport. The document is now exactly one visual viewport tall and never scrolls, while every normal app page scrolls inside the clipped opaque pane. The failed 192 px BottomNav extension was removed and its original overflow behavior restored.
- Responsive browser QA PASS at 390×844 and 430×932: document `scrollHeight === clientHeight`, document/window scroll remains 0, the app pane scrolls from 0 to 650 with full content height, the shell clips overflow, the nav remains fixed at the viewport bottom, horizontal overflow is absent, and no error-level console entries were observed.
- Validation: `npm run test` PASS (30 files, 319 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. The Tailscale app and worker both return HTTP 200, and served source contains the new app scroll viewport. Existing non-blocking bundle-size and stale Browserslist warnings remain.
- This workaround may keep Safari's floating toolbar expanded because the root document no longer scrolls. That is the intentional tradeoff required to guarantee no scrolling text can appear behind the translucent browser UI; installed Home Screen/native presentation is unaffected by browser chrome.

## Rev 22 — iPhone Safari toolbar opacity fix + Hyper-Dev Google diagnosis

- A real iPhone Safari screenshot showed `Calculate my targets` through Safari's translucent bottom address toolbar below Hyper's fixed navigation. The initial attempt extended an opaque paper-colored surface 192 px beneath the visual viewport. Desktop responsive geometry passed, but the subsequent real-device retest proved Safari clipped the extension and it did not solve the bug. Rev 23 supersedes this attempt.
- A second real-device screenshot returned Supabase HTTP 400 `Unsupported provider: provider is not enabled`. This conclusively identifies Google as disabled in isolated Hyper-Dev Auth, before Google or the post-login redirect is reached. Enable Google only in Hyper-Dev `nwvgkxqjqihqnjuworqz`, using its callback `https://nwvgkxqjqihqnjuworqz.supabase.co/auth/v1/callback`, and allow the private Tailscale app URL in Hyper-Dev Auth URL Configuration. Prefer a separate staging Google Web OAuth client; never copy, expose, or modify a production secret during this step.
- No remote auth setting was changed because Google client ID/secret entry requires the user's credential and an explicit Hyper-Dev configuration action. Production ref `nnwfaaxmyvqsdnfcdxom` was not targeted or changed.
- Validation after the UI fix: `npm run test` PASS (30 files, 319 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. Existing non-blocking bundle-size and stale Browserslist warnings remain.

## Rev 21 — live Hyper-Dev iPhone test services launched

- Started the real client on `127.0.0.1:5173` and the Codex/Claude photo worker on `127.0.0.1:8788` for the user's device test. Tailscale was already running with tailnet-only routes from `https://alexanders-macbook-air.taileaf222.ts.net/` to port 5173 and port 8443 to the worker.
- The repo had no local Supabase env file and the Codex shell had no inherited Vite variables. Replaced the initial incomplete client process with one using Hyper-Dev's publishable client key fetched in memory through the already-authenticated Supabase CLI. The key was not printed, written to disk, or added to the handoff.
- Verified the served client source identifies Hyper-Dev ref `nwvgkxqjqihqnjuworqz`, not production. Local/private app and worker health checks all returned HTTP 200. Current listener PIDs at launch were app `34419` and worker `29723`; these temporary processes end when their exec sessions or computer restart.
- Production ref `nnwfaaxmyvqsdnfcdxom` was not targeted or changed. No source, dependency, database, function, secret, staging configuration, commit, or push change was made in this launch step.

## Rev 20 — scoped pre-device nutrition UI polish completed

- Kept the deliberate Saved-first logger order, fully visible photo review, current navigation, WHOOP placement, Train flow, typography, and color system unchanged.
- Photo review now uses tighter always-expanded item rows with two-line nutrition/status metadata. The plate total, Retake, and `Log N items` actions stay pinned in an opaque footer; validation/save errors render in that footer with an assertive live alert. The footer fills the sheet's bottom safe-area padding while keeping controls above it.
- Fixed a 390 px horizontal overflow in each photo item's USDA source control by moving the left offset to a containing wrapper.
- Fuel meal headers now show a serving-adjusted, right-aligned kcal total for every visible group. Empty Unassigned remains hidden unless it has entries or is an active drag destination; meal collapsing was intentionally not added.
- Added global Motion `reducedMotion="user"` handling alongside the existing CSS reduced-motion rules, so transform-driven Motion animations respect the iPhone accessibility preference.
- Files added/changed for this batch: `src/components/nutrition/FoodLogger.tsx`, `src/components/nutrition/NutritionGroupLedger.tsx`, `src/components/nutrition/nutritionLogUtils.ts`, `src/main.tsx`, and `tests/nutritionLogUtils.test.ts`.
- Responsive browser QA PASS at true 390×844 and 430×932. Verified meal totals, always-visible review items, sticky actions while scrolling, footer/error visibility, final-field clearance, zero horizontal overflow, and opaque bottom safe-area coverage. Browser console errors were empty.
- Validation: `npm run test` PASS (30 files, 319 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. Existing non-blocking bundle-size and stale Browserslist warnings remain.
- No staging or production changes were made. The UI batch and prior rev-17/rev-19 source remain local and uncommitted pending real-device Hyper-Dev sign-off. Preserve unrelated user files `supabase/.temp/cli-latest` and `.claude/launch.json`.

## Rev 19 — independent-audit fixes completed (source + Hyper-Dev function)

- Addressed every accepted pre-device correctness finding from the independent read-only audit. UPC-E camera results now use the detector's `upc_e` format, expand to UPC-A with the GS1/ZXing rules, validate the expanded check digit, and compare/query equivalent USDA GTIN-12/13/14 values. EAN-8, UPC-A, and EAN-13 behavior is unchanged. Regression coverage uses UPC-E `04252614` → UPC-A `042100005264`.
- Finished runs now persist as recoverable seven-day summary snapshots. A new run is snapshotted immediately; finished snapshots clear only after successful save or explicit discard. Starting another run is disabled while an interrupted run awaits Resume/Discard.
- GPS save retries now look up and reuse the same user/source/type/`started_at` session before inserting, while stable segment IDs still upsert. A partial segment upsert or failed session link returns failure and leaves the summary available for retry instead of reporting a false success or creating an empty duplicate.
- Photo multi-item saves remove each successfully logged item immediately and retain the created food ID for a failed item's retry, so a later-item network failure cannot re-log earlier successful items. Review offers only gram-based USDA candidates; ml candidates are excluded because density is unknown.
- Changing a log date away from the Fuel page's selected day now clears the day-scoped group immediately, hides that day's destination selector, and explicitly shows that the target date will be Unassigned.
- Hygiene fixes: OAuth state MAC verification now uses Web Crypto `verify`; Settings uses router navigation for Start a run; the dormant Cronometer importer remains intentionally unreachable for provenance/reference; empty stale directories were not deleted because they do not affect the build.
- iPhone UI fixes: five food-method tabs use equal widths; empty Unassigned is hidden except during drag; meal rows use readable two-line layout and no hard truncation; photo review stacks name/grams below 420 px; WHOOP actions use a two-column row; frequent Fuel/History icon actions are 44×44 targets.
- Responsive browser QA PASS at true 390×844 and 430×932: all five logger tabs fit (measured within 25–359 px at 390), photo fields stack, photo macros/status wrap cleanly, empty Unassigned is absent, meal names remain readable, Fuel/History action targets measure 44×44, connected WHOOP actions remain single-line, and console errors are empty.
- Validation: `npm run test` PASS (29 files, 317 tests); `npm run lint` PASS; `npm run build` PASS; `node --check scripts/photo-food-worker.mjs` PASS; `git diff --check` PASS. Existing non-blocking bundle-size and stale Browserslist warnings remain.
- Deployed only the updated `food-lookup` source to isolated Hyper-Dev with explicit ref `nwvgkxqjqihqnjuworqz`. Remote listing reports ACTIVE version 2; an unauthenticated UPC-E request returns HTTP 401 `UNAUTHORIZED_NO_AUTH_HEADER`. The default production-linked project was never targeted and production remains untouched.
- Preserve unrelated user files `supabase/.temp/cli-latest` and `.claude/launch.json`. Source changes remain uncommitted pending real-device Hyper-Dev sign-off.

## Rev 18 — Hyper-Dev USDA function deployed (remote staging only)

- The user added `USDA_API_KEY` to Hyper-Dev secrets. No secret value was read, printed, copied, or stored locally.
- Safety check found this clone is still linked by default to production ref `nnwfaaxmyvqsdnfcdxom`. Deployment therefore used the explicit staging argument `--project-ref nwvgkxqjqihqnjuworqz`; the default linked project was not targeted.
- Deployed only `food-lookup` to Hyper-Dev. Remote function listing reports `food-lookup` ACTIVE, version 1, `verify_jwt=true`. An unauthenticated POST returned HTTP 401 `UNAUTHORIZED_NO_AUTH_HEADER`, confirming the protected boundary.
- The live USDA secret has not yet been exercised with a real signed-in JWT. The first full iPhone test should search a common food and scan a valid barcode to confirm the configured key and USDA response end to end.
- Production source, database, functions, and secrets were not changed. The rev-17 source changes remain local and uncommitted. The project is ready for the independent read-only work/UI audit before full real-account testing.

## Rev 17 — two-angle photo review + barcode hardening (implemented and validated locally)

- Photo capture now accepts one required top view plus one recommended 45° side view, preserves color, sends up to two 2048 px images, and asks for measured plate diameter plus oils/sauces/dressings/hidden ingredients. The private worker cross-checks both views, prevents duplicate counting, requires exactly one top image, accepts at most one side image, and deletes private temporary files after each job.
- Claude defaults to the authenticated `fable` alias; Codex remains `gpt-5.6-sol`. Local worker health confirmed both CLI providers installed and authenticated with those configured models. No real-meal inference was run in this revision, so accuracy still requires the weighed-meal iPhone benchmark.
- Photo review no longer auto-selects the first USDA text match. Every model estimate remains explicit; up to three gram/ml USDA candidates are shown per item and only affect macros after the user chooses one. Preview now supplies deterministic USDA fixtures so this confirmation UI works without a deployed function or key.
- The existing no-cost ZXing-C++ WebAssembly scanner remains the recommendation. Camera capture now requests 1920×1080 at 30 fps, applies continuous autofocus when the browser exposes it, scans every 180 ms, retains GTIN checksum validation/torch/manual fallback, and removes the non-FOLIO glow.
- Add `USDA_API_KEY` to the Hyper-Dev Edge Function secrets now and deploy only `food-lookup` there. Do not add FatSecret credentials yet: wait for Premier Free approval and review its storage/attribution terms before adapting Hyper's persisted food rows. FatSecret uses OAuth client credentials, not the existing USDA-key path.
- Files changed: `scripts/photo-food-worker.mjs`, `src/components/nutrition/BarcodeScanner.tsx`, `src/components/nutrition/FoodLogger.tsx`, `src/lib/photoAnalysis.ts`, `src/preview/mockSupabase.ts`, `tests/photoAnalysis.test.ts`, and `tests/previewSupabase.test.ts`.
- Validation: targeted food/photo tests PASS (5 files, 12 tests); full `npm run test` PASS (29 files, 312 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax and `git diff --check` PASS. Browser QA at 390×844 and 430×932 confirmed the two-angle flow and review fit without horizontal overflow. A preview-only missing-Functions error found during QA was fixed with the deterministic mock and regression test; the browser session was not rerun after that final mock fix. Existing bundle-size and stale Browserslist warnings remain.
- Production and Hyper-Dev were not touched. Preserve unrelated user changes in `supabase/.temp/cli-latest` and `.claude/launch.json`.

## Rev 15 — Eufy scale import deferred to native Apple Health (implemented and validated locally)

- Product decision: do not ship or test the Eufy → Fitbit → Google Health bridge in the web app. When Hyper becomes a native iPhone app, use the shorter EufyLife → Apple Health → Hyper HealthKit path instead.
- Removed the Eufy connector and weight card from Settings/Dashboard; removed the Google Health client, OAuth/sync Edge Functions, shared helpers, function config, database/client types, schema tables/policies/indexes, migration, and normalization tests.
- No Google Health migration or function had been deployed to Hyper-Dev or production, so no remote cleanup is required. `food-lookup` and all barcode/USDA work remain intact.
- Native implementation requirements for later: an Xcode-signed target, HealthKit capability and read-purpose text, permission for body mass (and body-fat percentage only if desired), anchored/background queries, provider-neutral measurement storage, and explicit source/deduplication handling.
- Safety checkpoint: `45f1bd2d refactor(health): defer scale sync to native app`.
- Validation after removal: `npm run test` PASS (27 files, 309 tests); `npm run lint` PASS; `npm run build` PASS; worker syntax, active-reference scan, and `git diff --check` PASS. Browser QA at 390×844 confirmed Today and Settings have no Eufy/Google Health remnants, no horizontal overflow, and no console errors. Existing bundle-size and stale Browserslist warnings remain.
- Production and Hyper-Dev were not touched. Preserve unrelated `supabase/.temp/cli-latest` and `.claude/launch.json` changes.

## Rev 14 — native barcode capture (implemented locally; staging deploy pending)

- Added a touch-first iPhone barcode scanner to Log Food. It uses maintained `barcode-detector@3.2.1` with ZXing-C++ WebAssembly, is lazy-loaded, decodes UPC-A/UPC-E/EAN-8/EAN-13 on-device, validates GTIN checksums, supports rear camera/flashlight/manual digits, and serves the WASM from Hyper rather than a CDN. Exact barcode matches are marked `Barcode … · USDA verified` and logged with barcode provenance.
- Replaced browser-exposed USDA access with authenticated `food-lookup`; `USDA_API_KEY` now remains in an Edge Function secret. USDA FoodData Central is the current free lookup source. FatSecret Premier Free remains the planned licensed primary source after approval; Open Food Facts was not mixed in because it is crowd-sourced and its ODbL/share-alike terms require a deliberate product decision.
- Research conclusion: Scandit/Scanbot are likely stronger commercial decoders for damaged/distant codes, but require commercial licensing. The current ZXing-C++ WASM route is the best maintained free fit for an iPhone PWA and preserves a clean switch point for a future native Apple VisionKit/ML Kit scanner.
- New authenticated `food-lookup` Edge Function requires only the `USDA_API_KEY` Hyper-Dev secret. No Google Health configuration is part of staging anymore.
- Browser QA at 390×844 PASS for Log Food's five tabs, scanner layout, and manual fallback; no console errors. Real iPhone camera and barcode lookup remain staging E2E tests because `food-lookup` is not deployed.
- Safety checkpoint: `c13c05bf feat(app): harden iPhone logging and health sync`. Validation: `npm run test` PASS (28 files, 312 tests); `npm run lint` PASS; `npm run build` PASS; `node --check scripts/photo-food-worker.mjs` PASS; `git diff --check` PASS. Existing non-blocking bundle-size and stale Browserslist warnings remain. `npm audit --omit=dev` reports 8 advisories in pre-existing application dependencies (1 low, 1 moderate, 6 high), including Vite/react-router chains; scanner dependencies are not implicated. Dependency remediation needs separate approval. Production was not touched. Preserve unrelated `supabase/.temp/cli-latest` and `.claude/launch.json` changes.

## Rev 13 — iPhone nutrition/GPS usability pass (implemented locally; barcode work completed in rev 14)

- Current uncommitted source changes make the bottom navigation and home-indicator safe area explicitly opaque, add animated/action-specific WHOOP progress and completion announcements, and open Edit Entry directly on editable name/calorie/protein/carbs/fat fields.
- Log Food now opens with a first-class Saved tab beside USDA/Manual/Photo. USDA, manual, saved, OpenAI photo, Claude photo, legacy Cronometer, and future barcode logs display compact provenance; missing/legacy source values safely display as Manual.
- Meal sections have touch-friendly earlier/later controls. Numbered meals/snacks are renumbered from their current chronological order; Breakfast/Lunch/Dinner order is enforced during creation and reordering.
- Removed the user-facing Cronometer CSV button. Official Cronometer documentation confirms automatic Food export to Android Health Connect, but its iPhone Apple Health documentation does not offer nutrition export; an iPhone web app also cannot use native HealthKit. Do not scrape Cronometer credentials. The supported automatic replacement is direct Hyper barcode scanning with USDA now and FatSecret Premier later.
- Reliable Safari barcode decoding needs a maintained decoder because native `BarcodeDetector` is not dependable in iPhone Safari. Rev 14 implements this with `barcode-detector`/ZXing-C++ WASM rather than the older `@zxing/browser` candidate.
- Photo preview is now color (analysis was already color), capture guidance recommends a known plate diameter or a clean ruler/printed 5 cm marker on the food plane, and the worker prompt uses known scale while rejecting unmeasured utensils. The private worker now pins OpenAI `gpt-5.6-sol` and Claude `opus`, exposes the selected model in health/results, and the UI displays it. Restart the worker after promotion.
- GPS now rejects reported speeds below 1.2 m/s for this run-specific tracker and, when device speed is missing, requires displacement to exceed 1.3× the combined uncertainty of both fixes. Restored older runs merge current defaults. Regression tests cover iPhone-like fidget speed and missing-speed drift.
- Validation: targeted tests PASS; `npm run test` PASS (25 files, 304 tests); `npm run lint` PASS; `npm run build` PASS; `node --check scripts/photo-food-worker.mjs` PASS; `git diff --check` PASS. Existing non-blocking bundle-size and Browserslist warnings remain. Production was not touched. Preserve unrelated `supabase/.temp/cli-latest` and `.claude/launch.json` changes.

## Rev 12 — built-in GPS replaces paid Strava connector (implemented; validated)

- Product decision: Strava's API application form is disabled for the user's free account and states API access requires a subscription. The user chose to remove Strava rather than add a paid dependency. No purchase was made.
- Removed the Strava Settings connector, OAuth return handling, History sync path, store state/actions, client/import/sync libraries, preview fixture, tests, Edge Functions, function config, client database type, schema tables/policies, and the unapplied Strava migration. Production was not touched.
- Settings now presents `iPhone GPS` as built in with no paid account and links directly to `/train/run`. History sync is WHOOP-only. GPS recordings remain the primary event; WHOOP enriches overlapping GPS sessions with strain, heart rate, and calories.
- Kept `strava` as a legacy `ActivitySource` value so any old imported activity can remain readable without retaining the live connector. New production schemas no longer create Strava connection/token tables.
- Validation after removal: `npm run test` PASS (25 files, 299 tests), `npm run lint` PASS, `npm run build` PASS. Existing non-blocking build warnings remain: large bundle and stale Browserslist data.
- Isolated staging is `Hyper-Dev` ref `nwvgkxqjqihqnjuworqz`; production remains `nnwfaaxmyvqsdnfcdxom` and was never targeted. Staging cleanup of the already-applied Strava tables/functions/secret is the next action.

## Rev 10 — production-like sandbox entry (implemented; browser-verified)

- Added dev-only `/sandbox`: it latches the existing in-memory Supabase backend, redirects directly to the normal Dashboard, and renders the real app routes without the `/preview` gallery/banner. Sandbox mode hides preview-only Cronometer/photo samples and GPS simulator controls, so the visible UI matches the app. `/preview` remains the fixture/simulator laboratory.
- Sandbox photo logging calls the real configured local worker instead of the mock result. Start with `npm run photo-worker:sandbox` (explicitly enables the preview-token bypass for private testing only) plus `npm run dev -- --host 127.0.0.1`. Settings health verified both OpenAI and Anthropic installed/authenticated. On iPhone, replace localhost worker URL with its private Tailscale HTTPS URL.
- Integration boundary: real Codex/Claude inference, a real user-exported Cronometer Servings CSV, and real iPhone GPS for Free/Intervals/Sprints can be tested in `/sandbox` while writes stay in memory. WHOOP/Strava buttons use the real UI but fixture transports in sandbox; real OAuth/account imports still require isolated Supabase staging because they need genuine JWTs, deployed callbacks/functions, secrets, and remote token tables.
- Browser verification: `/sandbox` redirected to the normal Dashboard; Fuel contained no sample buttons; worker health reported both providers signed in; Run showed only Free/Intervals/Sprints with no simulator selector; 390×844 viewport had no horizontal overflow; no console errors. Final validation remains 313/313 tests PASS, lint PASS, build PASS.
- Phone setup verified: Mac Tailscale is running; `https://alexanders-macbook-air.taileaf222.ts.net/sandbox` returns HTTP 200 through the private app route; `https://alexanders-macbook-air.taileaf222.ts.net:8443/health` proxies to the worker and reports OpenAI + Anthropic installed/authenticated. Keep both terminals and Tailscale running, set the in-app worker URL to the port-8443 origin, and keep this on private Serve, never public Funnel.

## Rev 9 — nutrition groups, Cronometer CSV, and local photo worker (implemented; sandbox-verified)

- Product constraint: iPhone web app/PWA is the primary runtime, with a possible future native wrapper/app. Preserve touch-first controls, iOS Safari camera/file input compatibility, safe-area and responsive behavior, HTTPS-only phone features, and portable provider/data interfaces. The private Codex/Claude subscription worker runs on the owner's Mac and must be exposed to the iPhone through an authenticated HTTPS route for testing; never assume `127.0.0.1` on the phone reaches the Mac. Keep nutrition capture/domain logic separate from browser transport so a future native camera/barcode client can reuse it.
- Added additive migration `20260716120000_add_nutrition_groups_and_imports.sql`: owner-scoped `nutrition_groups` and `nutrition_import_batches`, nutrition-log group/order/source/external-id provenance, food external identities, RLS, legacy breakfast/lunch/dinner/snack backfill, and idempotency indexes. Mirrored in `supabase/schema.sql` and client types. Migration is local only and has not been applied to any remote project.
- Fuel now supports unlimited day-scoped named groups (Breakfast, Lunch, Dinner) plus independently numbered Meal 1/2/… and Snack 1/2/…. Foods can be moved by desktop drag/drop or the accessible move sheet used on mobile; source badges distinguish Cronometer, OpenAI photo, Claude photo, and barcode entries.
- Added idempotent Cronometer **Servings CSV** import. Parser handles quoted CSV, current underscore-style headers, dates/times, embedded units, diary-group mapping, duplicate files/rows, and retry cleanup after a failed batch. Imported foods preserve exported macros/serving quantities and map to named/numbered groups.
- Reworked photo logging into a multi-food review: the model returns separate foods with editable grams/confidence/notes; the client best-effort resolves each component through USDA when configured, otherwise clearly retains the model estimate. Users may remove items, choose a group/date/time, and log all items with provider provenance.
- Added private local worker `scripts/photo-food-worker.mjs` plus strict JSON schema and `npm run photo-worker`. It verifies the app's Supabase JWT, restricts origins, stores each photo in a private temporary directory, invokes local Codex or Claude in safe/read-only mode, and deletes the job directory afterward. The app stores only worker URL/provider selection in local storage; provider credentials remain in their CLIs.
- Actual worker verification: health reports both CLIs installed and authenticated through the owner's subscriptions. The same 512×512 synthetic plate completed through both providers and returned valid separate rice/chicken/broccoli JSON. Claude correctly treated the illustration as low-confidence (0.20 each); OpenAI returned moderate confidence (0.58–0.62). Fixed Claude CLI compatibility by removing only the unsupported Draft 2020 `$schema` metadata from its strict schema argument; Codex continues using the original schema. This verifies worker/auth/schema wiring, not real-photo portion accuracy; the next model test must use an actual iPhone meal photo.
- Browser verification in `/preview`: created Meal 1 and Dinner while preserving Breakfast/Lunch/Snack 1; moved oats into Meal 1; imported the 4-row Cronometer sample with provenance; analyzed a preview plate into 3 editable items and logged all to Meal 1 with OpenAI provenance; changed/restored provider; checked the Fuel ledger at 390×844. No browser console errors.
- Final validation after all edits: `npm run test` PASS (27 files, 313 tests), `npm run lint` PASS, `npm run build` PASS. Existing warnings only: bundle >500 kB and stale Browserslist data. `git diff --check` PASS. `supabase db push --dry-run` PASS and lists exactly the four activity/integration migrations plus the new nutrition migration; nothing was pushed.
- Nutrition/photo safety checkpoint committed locally as `5a78c734 feat(nutrition): add grouped imports and subscription photo logging`. Unrelated pre-existing changes remain untouched: `supabase/.temp/cli-latest` and `.claude/launch.json`.
- Test readiness: preview/sandbox is ready now. Real signed-in nutrition, WHOOP, and Strava E2E still require an isolated remote Supabase staging project, applying the five pending migrations there, deploying the four OAuth/sync functions, configuring provider app credentials/callbacks, and starting the local photo worker. Do not push these migrations to production first.

## Rev 8 — unified food inbox, dynamic meals, and barcode-source decision (research/design only)

- Proposed nutrition UX: every captured item enters a day-scoped food inbox with source/provenance (`photo_openai`, `photo_anthropic`, `cronometer_csv`, `barcode`, `manual`). Users can create unlimited ordered containers labeled Meal 1, Meal 2, … and Snack 1, Snack 2, …, then move/reorder items between containers; unassigned items remain visible in the inbox.
- Current `nutrition_logs.meal_type` only supports a single text tag and cannot represent dynamic containers or ordering. Implementation requires an additive meal-container table plus nullable container/order/source/external-id fields on nutrition logs, with backward-compatible mapping for breakfast/lunch/dinner/snack.
- Photo analysis must return multiple editable components (food candidate, estimated grams/range, confidence, notes), not the current single aggregate with model-invented macros. Resolve each component against USDA/FNDDS or another licensed food source before saving totals.
- Subscription-backed OpenAI and Anthropic adapters can share a local Mac worker/provider interface. OpenAI Codex subscription auth supports trusted private automation, local image inputs, and JSON-schema output, but OpenAI still recommends API keys for normal automation. Anthropic technically supports subscription OAuth for scripts, but its current legal guidance says developers building products/services should use API authentication; keep any subscription adapter owner-only and experimental.
- Cronometer Basic provides free barcode scanning, curated CRDB entries, Nutritionix/USDA branded results, diary groups, and raw CSV export, but no normal live consumer API. Therefore Cronometer capture can initially sync through idempotent CSV import, not real-time OAuth.
- Free barcode alternatives: MyNetDiary advertises more than 2 million staff-verified foods and free scanning, but its commercial API is expensive and unsuitable for this integration. fatsecret advertises 2.3 million human-verified foods, free scanning, and an official developer API; eligible startups can apply for free Premier access including barcode lookup, with attribution. For hyPer-native scanning, fatsecret Premier Free primary plus USDA fallback is a stronger integration path than routing every scan through Cronometer.
- Recommended implementation sequence in isolated staging: (1) dynamic meal/snack containers + inbox, (2) Cronometer CSV importer/dedupe, (3) multi-item photo draft/review with OpenAI local worker first, (4) optional Anthropic local adapter, (5) native hyPer barcode scanner via fatsecret/USDA.

## Rev 7 — photo nutrition, barcode, and Cronometer research (read-only product decision)

- Photo logging already exists in both clones. `src/components/nutrition/FoodLogger.tsx` captures/compresses an image and calls the deployed `supabase/functions/process-food-photo`; the function currently uses Google Vertex/Gemini and directly estimates name, serving, calories, and macros. USDA text search is already implemented.
- Recommended accuracy change: use the vision model to identify meal components and estimate portions, then resolve nutrients from a canonical database and require user confirmation. Do not treat model-generated macro values as authoritative. Research consistently identifies food quantity/portion scale as the difficult part; database-grounded multimodal pipelines outperform relying only on model memory.
- Barcode scanning does not require Cronometer. Add an HTTPS camera scanner with a cross-browser decoder fallback, then query USDA FoodData Central by GTIN/UPC. Cronometer itself documents USDA and Nutritionix as barcode sources. Open Food Facts is a possible fallback, but its ODbL attribution/share-alike requirements need a deliberate licensing decision before mixing its data into hyPer's food database.
- Normal Cronometer accounts have CSV export, including timestamped food/recipe entries, but no public self-service user API was found. API access is described for Cronometer Pro Plus/partners. Android offers a better future bridge because Cronometer explicitly exports Food/Water to Health Connect; iOS HealthKit access requires a native iOS target and Cronometer's public documentation does not clearly promise nutrition export.
- Subscription-funded model execution is feasible for a private single-user prototype, not the correct basis for a public hosted product:
  - Anthropic is the clearest supported personal automation route: Claude subscriptions can authenticate `claude -p`/Agent SDK with OAuth, including a long-lived inference token and a separate Agent SDK credit allowance. Anthropic explicitly prohibits third-party products from routing users through consumer subscription credentials.
  - OpenAI Codex supports ChatGPT subscription auth, structured output, and image inputs through its local SDK. Official guidance frames this as local/coding-focused trusted automation and says API-key auth is the normal automation/product path; consumer terms also restrict programmatic extraction.
  - Kimi membership provides a subscription-scoped, OpenAI/Anthropic-compatible Kimi Code API key with image support, but Kimi explicitly directs product integration and multimodal applications to its separate pay-as-you-go Platform.
- Best private-beta architecture: phone uploads a short-lived photo job to staging; a trusted Mac worker authenticated to the owner's subscription processes it; structured candidates return to hyPer for database grounding and confirmation. This requires the Mac to be online and inherits subscription quotas, latency, auth, and policy limits. For multiple users or reliable production, use a normal provider API/business plan or an open-weight model hosted under hyPer's control.

## Rev 6 — release-readiness audit (read-only; no live changes)

- `/Users/alex/Desktop/hyPer-current-ui` is the correct promotion source: branch `codex/activity-sessions-current-ui` is based on `origin/main` at `0da91009`. Feature safety checkpoint: `330994ff feat(activity): add multi-source activity hub and GPS tracking`.
- `/Users/alex/Desktop/hyPer` is a stale, separate clone on `codex/activity-sessions-mvp`, based on older commit `f0f3ebba`, with overlapping uncommitted Phase 1 activity edits. Do not merge or copy that work back into the current branch.
- Fresh validation on 2026-07-16: `npm run test` PASS (25 files, 303 tests), `npm run lint` PASS, `npm run build` PASS. Existing warnings only: bundle >500 kB and stale Browserslist data.
- Preview smoke check: `/preview` loads; Settings shows WHOOP and Strava rows; WHOOP preview Connect changes status to `Connected • never synced` and exposes Sync/Disconnect. The existing handoff remains the evidence for the fuller fixture lifecycle.
- Linked Supabase project `nnwfaaxmyvqsdnfcdxom`: the four activity/WHOOP/Strava migrations are still pending; only `process-food-photo` is deployed. No WHOOP, Strava, or `APP_BASE_URL` secrets are configured. No production mutation was performed.
- Remote schema lint reports two pre-existing warnings in `public.save_split_snapshot` (text to `uuid[]` assignment); unrelated to this branch.
- No CI workflow, hosting config, or staging environment is declared in the repository. Supabase Branching or a separate staging project is the safest place for real OAuth E2E before production.
- Tailscale is stopped and has no serve config. Phone GPS testing needs Tailscale started, the IPv4 Vite server, and HTTPS serve restored.
- Cronometer/Cronometer import is not implemented in either clone. Current public material does not expose a normal self-service user-data API comparable to WHOOP/Strava; treat this as a separate discovery/import project, likely beginning with supported export formats or a partner-access request.
- Current Strava disconnect uses the legacy `/oauth/deauthorize` endpoint. It remains supported during the transition, but Strava now recommends `/oauth/revoke`; update/test this before broad release.

## Rev 5 — stationary GPS drift fix + pause/resume (sandbox-verified; 303 tests PASS, lint PASS, build PASS)

User field-tested the tracker and hit "distance increases without moving" + wanted pause buttons. Root cause: a flat 2 m jitter floor let standing GPS drift (±several m) accumulate. Fix + pause both in `runTracker.ts` (pure), exposed via hook + UI.

- **Drift fix** (`advanceTracker`): new config `stationarySpeedMps=0.6` and `driftAccuracyFactor=0.75`. When the device reports speed (`coords.speed`, Doppler-based, reliable on iOS/Android) below the stationary threshold → re-anchor, bank NO distance (kills standing drift). When speed is absent → jitter floor becomes `max(minStepM, accuracy*0.75)` so a step must clear the fix's own uncertainty. Real movement with reported speed uses the normal 2 m floor. Also fixed: after re-anchor (`lastPoint=null`, from pause or resume), the first sample now becomes the anchor instead of the `if(!prev) return` dead-ending.
- **Pause/resume**: state adds `pausedAtMs`, `totalPausedMs`, `lapPausedMs`; `Lap` gains `pausedMs` + `lapActiveSeconds(lap)`. `pauseTracker`/`resumeTracker`/`isPaused`. While paused, `advanceTracker` ignores samples (distance+clock freeze); `elapsedSeconds`/`currentLapSeconds`/`finishTracker` subtract paused time (saved `duration_seconds` = active/moving time); resume re-anchors so the gap (or being carried) never counts; manualSplit no-ops while paused. Crash-recovery `restoreTracker` closes an open paused span and defaults new fields (back-compat).
- Hook `useRunTracker`: `paused` + `togglePause()`. RunTracker page: Pause/Resume button beside Hold-to-finish in ALL modes; status shows "paused"; pace shows "—" paused; interval screen-tap-split disabled while paused; summary uses `lapActiveSeconds`.
- New sim scenario `stationaryDrift` ("Standing still (drift)") in gpsScenarios + preview source picker — reproduces the bug and proves the fix on-screen.
- Tests (+5, 21 total in runTracker): standing drift <5 m over 120 s; noisy-but-real movement still tracks; pause freezes dist+clock and resume excludes paused span; resume-after-carry banks no gap; split ignored while paused.
- Sandbox e2e (shipped module, in /preview): standing-still 120 s → **0 m**; real 5k → 5027 m; pause: 203 m/60 s frozen through 45 s pause, then 413 m/120 s after resume (45 s excluded). Live UI shows Pause↔Resume + "PAUSED".
- Answer to "is this feasible in a web app": YES — geolocation gives lat/lon/accuracy/speed; drift is a filtering problem every GPS app (incl. Strava) solves the same way. Thresholds remain exported constants for field tuning.

## Rev 4 — Strava importer + shared OAuth (sandbox-verified; 298 tests PASS, lint PASS, build PASS)

Full Strava integration mirroring WHOOP, plus the cross-source merge extended so a run recorded in-app OR imported from Strava carries WHOOP strain/HR/kcal as ONE event. NO live changes — still gated; remote has zero new migrations.

- Shared `supabase/functions/_shared/oauth.ts`: CORS, jsonResponse, HMAC-signed OAuth state. `whoop.ts` re-exports these (dedup); `strava-oauth`/`strava-sync` import them directly.
- Migration `20260711093000_add_strava_connections.sql`: `strava_connections` (owner-read metadata) + `strava_tokens` (zero policies + REVOKE, service-role only) — identical security model to WHOOP. Mirrored in schema.sql + supabase.ts Database type + `StravaConnection` in types.
- `config.toml`: `strava-oauth` verify_jwt=false (browser callback), `strava-sync` true.
- Edge Functions: `strava-oauth` (action=start → authorize URL scope `read,activity:read_all`; GET /callback → code exchange → service-role upsert tokens+connection → 302 `${APP_BASE_URL}/settings?strava=connected|error`; action=disconnect → deauthorize + delete). `strava-sync` (dumb proxy: load tokens, refresh if <5min — Strava ROTATES refresh tokens, persist new pair BEFORE fetch; GET /api/v3/athlete/activities per_page=50 with after/before epoch; 401→refresh+retry, 429→rate_limited; returns raw {records, nextPage}).
- Pure libs: `stravaImport.ts` (mapStravaSport, stravaLocalDateKey via utc_offset, normalizeStravaActivity → moving_time as duration, elapsed→ended_at, id→external_id; groupStravaSegments: per-activity, membership-first-else-create, carries Strava `name` as title, buildStravaPatch never overwrites HR that Strava didn't record so WHOOP-filled HR survives, no orphan deletion). `stravaSync.ts` (runStravaSync orchestrator; after creating a session, absorbs an overlapping auto WHOOP session via findAbsorbableWhoopSession — relink segments + fill-only metric copy + delete).
- `stravaClient.ts` prod transport; `stravaFixtures.ts` (batch1: long run 06:50 3d-ago [SAME window as WHOOP fixture wf-run-1] + ride; batch2 dup; batch3 fresh run ~30min-ago). Store: stravaConnection state + fetch/connect/disconnect/syncStrava (preview→fixtures, prod→edge fn).
- Settings: Strava row added under Connected services (WHOOP row + hairline + Strava row); `?strava=` return handling. History Sync button now runs BOTH providers (Strava first as hosts, then WHOOP enriches) — renamed `handleSyncSources`, gate `syncAvailable = preview || whoopConnection || stravaConnection`.
- Tests: stravaImport (11: sport map, tz date-key, normalize, per-activity create, no-op, HR-preserve update, user_edited skip), stravaSync (3: import+idempotence, WHOOP absorption into Strava run, non-overlap no-absorb).
- Sandbox e2e verified via store's own supabase client (NOT direct previewTables import — that reads a stale module instance in the embedded browser; always read back through `supabase.from()`): syncStrava + syncWhoop → the shared 06:50 long run is ONE session, `source:strava`, distance 8210m (Strava GPS), strain 12.5 / 158bpm / 621kcal (WHOOP), segSources ["strava","whoop"]. Both connectors render in Settings.
- Go-live for Strava (Gate B'): register Strava API app at strava.com/settings/api (Authorization Callback Domain = the Supabase functions host `nnwfaaxmyvqsdnfcdxom.supabase.co`), then `supabase secrets set STRAVA_CLIENT_ID STRAVA_CLIENT_SECRET STRAVA_STATE_SECRET=$(openssl rand -hex 32)` (+APP_BASE_URL already set for WHOOP), `supabase functions deploy strava-oauth strava-sync`.

## Rev 3 — cross-source merge (sandbox-verified; 284 tests PASS, lint PASS, build PASS)

User requirement: a run recorded in-app (or later via Strava) + the same run recorded by WHOOP must become ONE hyPer event carrying GPS pace/splits AND whoop strain/HR/kcal. NO live changes made — user explicitly re-gated: everything sandbox-first; the interrupted `supabase db push` never ran (remote still has zero new migrations; verified via `supabase migration list`).

- `whoopImport.ts`: `OVERLAP_MERGE_RATIO = 0.6` (share of the shorter window), `overlapRatioOfShorter`, `findEnrichmentHost` (best-overlap non-whoop session), `buildEnrichmentPatch` (strain/avg_hr/max_hr/energy_kcal ONLY — never type/times/GPS distance; user_edited hosts are fill-only), `findAbsorbableWhoopSession` (whoop auto, not user_edited). `groupSegments`: membership vote first; unmatched clusters try enrichment host before creating; membership pointing at a non-whoop session takes the enrichment branch (metrics-only) instead of the full whoop patch.
- `whoopSync.ts` port renamed `fetchSessionsInWindow` (ALL sources — hosts needed); store impl dropped the `source=whoop` filter.
- `appStore.saveTrackedRun`: after create+link, absorbs the best-overlap auto whoop session (relink its segments → copy metrics → delete it), so save-after-sync also converges to one event.
- History `ActivityLedgerRow`: splits table renders only `segment.source === activity.source` (foreign-source segments contribute metrics, not rows); chevron count uses the filtered list.
- Fixtures batch 5 (`wf-live-run`): whoop record spanning [now−40m, now−1m], strain 11.3 / 168 / 190 / 1850 kJ, no distance — overlaps any just-saved sandbox run.
- Tests: enrichment (create-suppression, metrics-only patch, idempotence, user_edited fill-only, sub-ratio → create), absorption finder, overlap math, FakeData e2e both syncs.
- Sandbox e2e verified BOTH directions via store calls in /preview: run-then-sync → `updated:1, created:0`, gps session gains strain/HR/kcal, keeps 5200m/1200s, whoop segment linked, 0 duplicates; sync-then-save → whoop auto session absorbed (deleted), segment relinked, metrics copied. Calendar shows "Run • GPS • 3.23 mi • 168 bpm • strain 11.3".
- Strava note: no Strava importer exists yet; when built, its sessions (source 'strava' with start/end) get the same enrichment for free.
- To demo on phone: save a tracked run in /preview, then press Sync 5× (batches clamp at 5); the 5th enriches the run.

## Rev 2 changes (validated: 277 tests PASS, lint PASS, build PASS)

- Pace now defaults to time per MILE everywhere (user request): `activityMetrics.ts` exports `MILE_M`, `paceSecondsPerMile`, `formatPace` → "7:32 /mi", `formatDistanceMi` (replaces km variants); `runTracker.ts` `rollingPaceSecPerMile`; History metrics/splits + RunTracker live/summary updated; sub-mile lap/segment distances stay in meters ("400 m"), aggregate distances in miles ("2.50 mi"). Verified in preview: seeded interval run shows "2.50 mi • 175 bpm", splits "7:03 /mi".
- Phone testing path (user has Tailscale installed, currently stopped): `tailscale serve --bg 5173` over the dev server gives an HTTPS ts.net URL — required because iOS Safari blocks geolocation + wake lock on insecure origins (plain LAN http URL cannot run the GPS tracker). Field-test via `/preview` → Run → "Real GPS" source (in-memory sandbox, real GPS, prod DB untouched).

## Rev 16 — pre-release food capture research (2026-07-16; research only)

- No source, database, staging, or production changes were made for this research. Production ref `nnwfaaxmyvqsdnfcdxom` remains off limits; isolated staging ref is `nwvgkxqjqihqnjuworqz`.
- Current barcode decoder (`barcode-detector`/ZXing-C++ WASM with UPC/EAN checksum validation, rear-camera/torch/manual fallback) remains the best no-cost fit for the iPhone PWA until a real-device bake-off proves otherwise. Scandit, Dynamsoft, and Scanbot are the commercial Web SDK candidates; future native iOS should evaluate VisionKit `DataScannerViewController`.
- USDA FoodData Central remains the server-side lookup fallback, not a complete branded-food solution. FatSecret Premier Free is the strongest planned database upgrade: its official API offers barcode lookup and a US dataset for qualifying startups, with attribution/terms. Open Food Facts is a useful optional global fallback only after accepting its crowd-sourced quality and ODbL obligations.
- A direct external-app alternative now exists: the free FatSecret consumer app includes barcode scanning, and FatSecret's OAuth 1.0 3-legged flow can authorize an existing FatSecret member so an app can read that member's food diary. This should be evaluated before building an indirect Cronometer bridge.
- Cronometer still has no self-service diary API identified. A new indirect path may be viable: Cronometer → Apple Health → Google Health → Hyper backend. Google Health's Nutrition Log API, `googlehealth.nutrition.readonly` scope, and webhooks can support a PWA backend, and Google documents importing Cronometer food on iOS. Individual-item name, serving, meal, and idempotency fidelity through the full chain are unverified; test a real account in Hyper-Dev before designing around it.
- Photo logging is a strong prototype, not a verified best-in-class system. OpenAI uses current flagship `gpt-5.6-sol`; the Claude worker uses `opus`, while Anthropic's current highest-capability public model is Fable 5. Existing limitations: one photo only, client resize to 1280 px, and the first USDA gram-serving search match can be accepted without a confidence gate. Highest-impact next improvements are top + 45°/depth capture, a known plate/reference size, visible component review, candidate/confidence gating, and a weighed-meal benchmark. Passio and LogMeal are the purpose-built comparison candidates.
- Required evaluation before a technology claim: 20–30 representative weighed meals and 30–50 varied barcodes on the target iPhone; compare detection recall, gram/calorie/macro error, latency, correction time, scan success, and false positives. Include OpenAI GPT-5.6, Claude Fable 5 if the subscription exposes it, current Claude Opus, and Passio/LogMeal trials.
- Release order: independent read-only code/UI audit → fix accepted P0/P1 findings → complete Hyper-Dev real-account tests (auth, WHOOP, food lookup/barcode, real photos, GPS field cases, persistence) → optionally spike FatSecret direct sync or Google Health nutrition sync if automatic external logging is a launch blocker → rerun test/lint/build → explicit production approval with migration/rollback plan.

## Current goal

Finish real-account E2E in isolated `Hyper-Dev`: built-in iPhone GPS + WHOOP enrichment, layered barcode lookup, researched OpenAI/Claude text/photo review, and dynamic nutrition groups. Eufy scale import is deferred until Hyper has a native HealthKit target. Staging sign-off gates every production change.

## Verified state

- Worktree `/Users/alex/Desktop/hyPer-current-ui`, branch `codex/activity-sessions-current-ui`, base `origin/main` @ `0da91009` (unchanged upstream).
- Full plan: `/Users/alex/.claude/plans/compressed-forging-fog.md` (user-approved).
- **Current validation: `npm run test` 331/331 PASS, `npm run lint` PASS, `npm run build` PASS, worker syntax PASS, and `git diff --check` PASS (2026-07-19).** Responsive browser QA passed at 390×844 and 430×932 with no error-level console entries, including the internal-scroll layout, daily meal defaults/numbering, direct saved-template edit/delete, AI food description, and layered barcode review. Both subscription-authenticated OpenAI and Claude description requests returned structured cited estimates in real worker E2E. Activity safety checkpoint exists at `330994ff`; nutrition/photo checkpoint exists at `5a78c734`. Isolated staging has the activity, WHOOP, nutrition work, and `food-lookup` ACTIVE v3; current client changes remain local. Hyper-Dev Google OAuth remains intentionally skipped by the user. Google Health/Eufy is removed. Production has none of this work.
- The detailed rev sections ABOVE (rev 5 drift+pause, rev 4 Strava, rev 3 merge, rev 2 miles) are the current source of truth; the per-phase notes BELOW are the original rev-1/2 record and predate those changes (e.g. they say km/277 tests and lack Strava — superseded).
- Phone testing: `npm run dev -- --host 127.0.0.1` (must bind IPv4 — the tunnel proxies IPv4; default is IPv6-only), then Tailscale serve exposes `https://alexanders-macbook-air.taileaf222.ts.net/`. HTTPS is required for iOS geolocation + wake lock. `vite.config.ts` `server.allowedHosts` allows the `.taileaf222.ts.net` domain (dev-only). Sandbox = `/preview` (fresh Safari tab; preview mode latches per-tab). Real GPS test: Run → Position source → "Real GPS".
- Sandbox note: the in-app browser pane throttles timers/animations (document reports hidden) — modal exits stick, 10× sim timing drifts; NOT app bugs. Read sandbox results back through the app's own `supabase` client (`supabase.from(...).select()`), NOT a direct `import('previewData')` — that reads a stale module instance.

### Phase 1 — segments data layer (verified in sandbox)
- `supabase/migrations/20260710090000_add_activity_segments.sql`: `activity_segments` (UNIQUE user_id+source+external_id, owner RLS) + `activity_sessions` aggregate columns (strain/avg_hr/max_hr/energy_kcal/distance_m) + flags (`auto_grouped`, `user_edited`, `dismissed_at` tombstone). Mirrored in `supabase/schema.sql` + `src/lib/supabase.ts` Database type.
- `src/preview/mockSupabase.ts` upgraded: mutations now PERSIST into previewTables; `upsert` honours `onConflict`; `.is()` treats undefined as null. This was a prerequisite — previously inserts/updates were dropped.
- `src/lib/activityMetrics.ts` (+tests): aggregateSegments (duration-weighted HR, strain=max documented approximation), formatPace/formatDistanceKm/formatClockDuration.
- History: activity rows show metrics line (km • bpm • strain) + expandable splits ledger (lazy segment fetch); chevron only when ≥2 segments; month fetch excludes dismissed.
- Verified: seeded 8-lap interval run renders metrics + 8 splits; added activity persists across month-nav re-fetch.

### Phase 2 — WHOOP grouping engine (verified in sandbox + 25 unit tests)
- `src/lib/whoopImport.ts`: normalize (kJ→kcal ×0.239006, tz-aware date key, PENDING_SCORE null metrics), clustering (running only, gap ≤ 360s, lap ≤ 900s), classification (≥2 reps: sprint if median ≤90s & ≥5 m/s else interval_run), membership-based reconciliation (majority vote via segment.session_id), user_edited/dismissed never patched or resurrected, orphan auto sessions deleted. Constants exported for tuning.
- `src/lib/whoopSync.ts`: port-injected orchestrator (paginate ≤10 pages → upsert segments → window re-read [since−7d, now] → apply plan).
- `src/preview/whoopFixtures.ts`: 4 sequential Sync batches (import / duplicates / late 9th lap / sprint day).
- Store `syncWhoop()` wires fixture transport in preview, Edge Function transport in prod. History gets quiet "Sync" action; delete of auto-grouped whoop session = soft dismiss; edit sets user_edited.
- BUG FOUND+FIXED via sandbox: segment upsert row must NOT include `session_id` (conflict update nulled links → sessions churned every re-sync). `upsertActivitySegments` omits it; linking owned by explicit updates only.
- Verified end-to-end in preview: batch1 "3 NEW" (8 laps→ONE interval_run 4.08km/173bpm + tennis + run), batch2 "UP TO DATE", batch3 "1 UPDATED" (9 splits, same session), batch4 sprint_session (6 reps), delete→re-sync stays gone (skippedUserEdited=1, verified via store call).

### Phase 3 — WHOOP OAuth (sandbox-verified UI; deploy gated)
- `supabase/migrations/20260710091000_add_whoop_connections.sql`: `whoop_connections` (owner SELECT only) + `whoop_tokens` (RLS zero policies + REVOKE ALL — client can never read tokens; not in client Database type).
- `supabase/config.toml` (new): `whoop-oauth` verify_jwt=false (callback is a browser redirect; auth in-code), whoop-sync/process-food-photo true.
- `supabase/functions/whoop-oauth/index.ts`: action=start (JWT → authorizeUrl w/ HMAC-signed state, 10-min expiry), GET /callback (verify state → code exchange → service-role upsert tokens+connection → 302 `${APP_BASE_URL}/settings?whoop=connected|error`), action=disconnect (best-effort revoke + delete). `_shared/whoop.ts` holds CORS/state/token helpers.
- `supabase/functions/whoop-sync/index.ts`: dumb proxy — loads tokens (service role), refreshes if <5min left (WHOOP ROTATES refresh tokens; new pair persisted BEFORE fetch), GET `/developer/v2/activity/workout?limit=25`, 401→refresh+retry once, 429→rate_limited, returns raw `{records, nextToken}`, stamps last_synced_at on final page. Endpoints/fields re-verified against developer.whoop.com 2026-07-11.
- `src/lib/whoopClient.ts` transport; Settings "Connected services 05" group (status line, Connect/Sync now/Disconnect, handles ?whoop= return); Session renumbered 06. History Sync gated on preview OR connection.
- Verified in preview: Connect→"Connected • never synced", Sync now→"Synced — 3 new, 0 updated.", Disconnect→"Not connected".

### Phases 4+5 — GPS run tracker (verified in sandbox + 16 unit tests)
- `src/lib/runTracker.ts`: pure wall-clock reducer. Accuracy gate ≤30m, 3-fix warm-up, teleport filter (>12.5 m/s, re-anchor after 3), 2m jitter floor that still accrues slow progress, 60s rolling pace clipped to current lap, auto-splits with interpolated crossing times, manual split w/ 700ms debounce, sprint hysteresis (≥5.0 m/s held 2s → rep; ≤3.0 m/s held 3s → end; discard <30m/<4s), stable runId for idempotent saves, localStorage snapshots (12h resume window). `finishedRunToActivity` → session(source gps) + per-lap/rep segments.
- `src/lib/gpsScenarios.ts`: deterministic scripts (steady 5k, 8×400, 6×sprints) shared by tests AND preview simulator.
- `src/hooks/useRunTracker.ts`: injectable PositionSource (real watchPosition vs simulator with CONSISTENT compressed clock), 1s tick, wake lock w/ visibility reacquire, throttled snapshots, cue playback. Bugs found+fixed via sandbox: tick effect must depend on isRunning boolean not state; simulator clock bases must init at creation (else startedAt = Date.now()×10); discard must clear `resumable`.
- `src/lib/runTrackerCues.ts`: WebAudio double-blip lap / rising sprint-start / falling sprint-end + vibrate (no-op iOS).
- `src/pages/RunTracker.tsx` at `/train/run` (chromeless; BottomNav hides; entries: Train header "Run" links ×2, preview gallery 08). Pre-start: mode tabs, auto-split SelectSheet, preview-only source picker. Live: hero pace/time/distance, lap/sprint panels, whole-screen tap-split, hold-to-finish 800ms. Summary: totals + splits table → Save/Discard. Resume banner on interrupted runs.
- Store `saveTrackedRun`: create session → upsert segments (stable ids) → link.
- Verified in preview: free run pace exactly 4:46/km, saved run appears in History ("Run • 5m • GPS • 0.94 km"); intervals: manual tap split resets live pace to "—", auto split exactly 400m, summary table correct incl. open final lap; sprints: 2 reps detected hands-free (89m, 7.5 m/s peak); resume banner after reload.

## Known risks / notes
- iOS PWA: GPS only tracks screen-on foreground (wake lock keeps it on); thresholds need field tuning (constants exported).
- Strain aggregation = max (non-additive) — documented approximation.
- v1 has no manual merge/split UI for wrongly-grouped sessions (type/title editable; delete tombstones work).
- Preview sim at 10× shows "gps weak" flickers in the throttled embedded pane only.
- App hosting still unknown → WHOOP redirect designed via Edge Function callback + APP_BASE_URL secret (set at deploy time).
- Cronometer import deliberately future: reuse source/external_id/raw-jsonb patterns (comment in segments migration).

## Complete file inventory (rev 5 — everything in this change)

Project ref `nnwfaaxmyvqsdnfcdxom`. All project changes are included in the local safety checkpoint on branch `codex/activity-sessions-current-ui`.

New migrations (unapplied, additive only):
- `supabase/migrations/20260630120000_add_activity_sessions.sql`
- `supabase/migrations/20260710090000_add_activity_segments.sql`
- `supabase/migrations/20260710091000_add_whoop_connections.sql`
- `supabase/migrations/20260711093000_add_strava_connections.sql`

New Edge Functions (undeployed) + config:
- `supabase/config.toml` (verify_jwt flags)
- `supabase/functions/_shared/oauth.ts` (CORS, jsonResponse, HMAC signed state — shared)
- `supabase/functions/_shared/whoop.ts` (WHOOP endpoints + token exchange/refresh; re-exports _shared/oauth)
- `supabase/functions/whoop-oauth/index.ts`, `whoop-sync/index.ts`
- `supabase/functions/strava-oauth/index.ts`, `strava-sync/index.ts`

New client libs:
- `src/lib/activityMetrics.ts` — aggregate/format (miles, pace /mi, clock)
- `src/lib/activitySessions.ts` — session title/duration helpers
- `src/lib/whoopImport.ts` — normalize + group + cross-source enrich/absorb
- `src/lib/whoopSync.ts` — WHOOP sync orchestrator (injected ports)
- `src/lib/whoopClient.ts` — prod WHOOP transport (edge fn)
- `src/lib/stravaImport.ts` — normalize + per-activity group
- `src/lib/stravaSync.ts` — Strava sync orchestrator + WHOOP absorption
- `src/lib/stravaClient.ts` — prod Strava transport (edge fn)
- `src/lib/runTracker.ts` — pure GPS engine (drift gate, pause, laps, sprints, persistence)
- `src/lib/runTrackerCues.ts` — WebAudio + vibrate cues
- `src/lib/gpsScenarios.ts` — sim scripts (steady5k, 8×400, 6×sprints, stationaryDrift)
- `src/hooks/useRunTracker.ts` — React wiring (PositionSource port, wake lock, pause)
- `src/pages/RunTracker.tsx` — chromeless tracker at `/train/run`
- `src/preview/whoopFixtures.ts`, `src/preview/stravaFixtures.ts` — sandbox transports

New tests (all passing): `tests/activityMetrics.test.ts`, `activitySessions.test.ts`, `whoopImport.test.ts`, `whoopSync.test.ts`, `runTracker.test.ts`, `stravaImport.test.ts`, `stravaSync.test.ts`.

Modified files:
- `src/App.tsx` (route `/train/run`), `src/components/shared/BottomNav.tsx` (hide on `/train/run`)
- `src/lib/supabase.ts` (Database types: activity_sessions cols, activity_segments, whoop_connections, strava_connections)
- `src/types/index.ts` (ActivitySession/Segment/Input, WhoopConnection, StravaConnection, activity types/labels)
- `src/stores/appStore.ts` (activity CRUD, segments, syncWhoop/syncStrava, connect/disconnect both, saveTrackedRun)
- `src/pages/History.tsx` (calendar activities, splits ledger, Sync both providers, soft-dismiss/user_edited)
- `src/pages/Settings.tsx` (Connected services: WHOOP + Strava rows, ?whoop=/?strava= return)
- `src/pages/Workout.tsx` (Run entry links), `src/preview/Preview.tsx` (gallery 08 Run)
- `src/preview/mockSupabase.ts` (mutations persist + onConflict upsert), `src/preview/previewData.ts` (seeded segments, empty connection tables)
- `supabase/schema.sql` (mirror all new tables/cols), `vite.config.ts` (allowedHosts for tailnet)
- `.claude/launch.json` (dev server launch config — untracked)

## Sign-off gates (nothing done yet — all gated on user approval)

1. **GATE A** (after isolated-staging sign-off) — apply the 3 activity/WHOOP migrations plus the nutrition migration to production project `nnwfaaxmyvqsdnfcdxom`. Do not use production as the first migration target.
2. **GATE B — WHOOP go-live**: user registers WHOOP dev app (redirect URL exactly `https://nnwfaaxmyvqsdnfcdxom.supabase.co/functions/v1/whoop-oauth/callback`, scopes `read:workout offline`) → `supabase secrets set WHOOP_CLIENT_ID=… WHOOP_CLIENT_SECRET=… WHOOP_STATE_SECRET=$(openssl rand -hex 32) APP_BASE_URL=…` → `supabase functions deploy whoop-oauth whoop-sync` → phone E2E + verify `whoop_tokens` unreadable as `authenticated`.
3. **GATE C — iOS field test**: permission prompt from Start gesture, wake lock stowed, pace stability, sprint threshold reliability, interval tap-split, cue audibility, kill→resume, Low Power Mode degradation, standing-still banks no distance, pause freezes clock. Tune exported threshold constants from real traces.

## Next action

Run the real iPhone/Hyper-Dev test matrix. First retest all seven barcodes and record the exact decoded/not-found state; test Describe with AI once under OpenAI and once under Claude, edit before log, verify Saved reuse/edit/history stability, then continue one/two-photo review, WHOOP enrichment, and Free/Intervals/Sprints GPS field behavior. Send failed barcode digits for provider-by-provider coverage checks. Record results before any production proposal. Open Food Facts must remain staging-only until ODbL compliance is resolved; FatSecret credentials/data remain gated on Premier Free approval and terms review; Eufy remains deferred to a native HealthKit app.
